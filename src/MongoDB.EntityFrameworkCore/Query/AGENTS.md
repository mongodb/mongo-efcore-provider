---
area: Query / LINQ translation
scope: ["src/MongoDB.EntityFrameworkCore/Query/**"]
reviewer-agent: query-reviewer
adjacent-areas: [Storage, Metadata, Serializers, "C# driver LINQ v3"]
---

# Query — AGENTS.md

## Scope

Translates `IQueryable<T>` against MongoDB-backed DbSets into MongoDB aggregation pipelines. There are **two translation paths**, chosen at compile time by the `MongoQueryMode` option (see *the gate* below):

1. **Native (default).** For the supported slice — single-collection **filter / sort / paging** over whole-entity results — the provider **generates the aggregation `BsonDocument[]` pipeline itself**, from a structured query expression tree, and runs it via `IMongoCollection<…>.Aggregate`. It does **not** go through the driver's LINQ provider for these.
2. **Driver-LINQ (gated fallback).** For everything outside the native slice (a **bare-entity, filtered, or correlated-beyond-FK** `SelectMany` over a reference navigation — a *projected, FK-correlated* reference `SelectMany` goes native, see the EF-347 slice 5 note below — a nested collection selector, or a whole-inner-entity projection — an owned-collection `SelectMany` projecting the element's members goes native, in either user-authored form — see the EF-347 slices 3–4 note below — a non-whole-entity, non-terminal, projected, or mismatched-entity-type `Intersect`/`Except` (a *whole-entity, terminal* `Intersect`/`Except` goes native via a source-tagging pipeline — see the EF-347 set-ops slice A note below; unlike everything else in this list, an out-of-scope `Intersect`/`Except` has no driver-LINQ baseline and hard-fails in every mode instead of landing here) — `VectorSearch`, a non-TPH or otherwise non-representable `OfType` — see the EF-347 note below — a bare-scalar or whole-entity `Distinct` (a *projected* `Distinct` goes native — see the EF-347 Task 2 note below) — a non-whole-entity, non-terminal, or mismatched-entity-type `Union`/`Concat` (a *whole-entity, terminal* `Union`/`Concat` goes native — see the EF-347 slice 2 note below) — the **non-native** `GroupBy` shapes — see the EF-SP6 note below — and the deferred Include shapes below), the provider still hands a driver-LINQ expression to the MongoDB C# driver's LINQ v3 provider, exactly as it always did.

> **As-built scope (EF-323 foundation + EF-329 predicate-breadth + EF-331 projection-pushdown + EF-332 layering + EF-SP4 scalar-cardinality sub-projects).** Native = filter/sort/paging, with **predicate breadth**: nullable equality/`== null` (IS NULL), collection `Contains` → `$in`/`$nin`, `string.StartsWith`/`EndsWith`/`Contains` → `$regularExpression`, and field-to-field/arithmetic comparisons → `$expr`. Native now also covers **terminal anonymous-type / DTO member-access projections** — a final `Select` whose body is a `new {...}`/DTO construction over top-level member accesses only — which emits a native `$project` stage instead of falling back; **bare scalar** projections (`Select(p => p.Name)`), **computed leaves** (arithmetic/method calls inside the projection), **mixed** entity+scalar shapes, and **non-terminal** `Select`s (followed by more query operators) still fall back to driver-LINQ. The remaining **computed long tail** — string transforms (`ToUpper`/`Substring`/…), date-part extraction, `Math.*`, and type-changing casts — is explicitly **out of scope for this sub-project**; it is a follow-on, not a gap left by predicate breadth.
>
> **Scalar cardinality (EF-SP4).** Native now also covers **entity reducers** (`First`/`FirstOrDefault`/`Single`/`SingleOrDefault`) via a synthesized `$limit` (1, or 2 for the `Single` family so the server-side reducer can still distinguish "more than one" from "exactly one") plus EF Core's own base cardinality reduction over the returned `IEnumerable<T>` — this is a DOM shaper, not a streaming one; and **scalar aggregates** (`Count`/`LongCount`/`Any`/`All`/`Sum`/`Min`/`Max`/`Average`) via a synthesized `$count`/`$group` stage plus a DOM scalar shaper and an explicit empty-input contract (`MongoEmptyAggregateBehavior`: default-value for `Count`/`LongCount`/`Any`/`All`/`Sum`, return-null for nullable `Min`/`Max`/`Average`, throw for non-nullable `Min`/`Max`/`Average`, matching the BCL LINQ contract). **What still falls back:** `Contains`/`ElementAt`/`Last` (no native form); computed aggregate selectors (`Sum(x => x.V * 2)` — the selector must be a plain member access); `All` with a comparison predicate (`All(x => x.V > 0)` — negating a comparison into query-native form is unsupported; ticket EF-335); and reducer/aggregate **streaming** (deferred — the `BsonRowReader` null/non-nullable-list gap means these still materialize via the DOM shaper, not the streaming one). (An aggregate that injects a predicate as a `$match` — `All`, or `Count`/`Any` with an unnormalized predicate — occurring AFTER a `Take`/`Skip` already applied to the same select USED TO force a fallback here; EF-347 Task 3 removed that guard once the ordered-op-list IR made tail-append always correct — see the Non-canonical Skip/Take note below.) Two retired cutouts from earlier sub-projects: the `ExecuteScalar` short-circuit is now gated on `NativePipeline == null` (rather than being unconditional), and the `resultCardinality != Enumerable` gate was relaxed to admit the new scalar/reducer cardinalities. (native `GroupBy` landed in EF-SP6; native `OfType` and projected `Distinct` in EF-347 slice 1; native whole-entity terminal `Union`/`Concat` in EF-347 slice 2; native whole-entity terminal `Intersect`/`Except`, via a source-tagging pipeline, in EF-347 set-ops slice A — see the notes below.)
>
> **OfType discriminator narrowing (EF-347, SP6-remaining slice 1).** Native now also covers `OfType<TDerived>()` over a TPH hierarchy: `TranslateOfType` builds a discriminator conjunct — a single-value `$eq` when `TDerived` is a leaf type, an `$in` over the discriminator subtree (`TDerived` and its own derived types) otherwise — via `TryBuildDiscriminatorPredicate`, and adds it to the tail `$match` through `AddPredicateConjunct` (composing with any other `Where` predicate already on the select), instead of unconditionally calling `MarkNotNativelyRepresentable()`. This works with **no shaper changes**: TPH entities are already streaming-ineligible and materialize through the DOM shaper via EF's own discriminator-based `MaterializationCondition`, which already picks the correct derived CLR type — the only thing native was missing was the `$match`/`$in` conjunct that keeps sibling-type rows out of the result. **Rejected by a guard → fall back (the guard PREVENTS native divergence from driver-LINQ):** `TryBuildDiscriminatorPredicate` returns `false` — so `MarkNotNativelyRepresentable()` is still called and the query falls back (throwing only under `NativeOnly`) — when (a) the target type has no discriminator property (non-TPH `OfType`, e.g. an unrelated-hierarchy narrowing), OR (b) the discriminator property has a **value converter or a non-default `BsonRepresentation`** (`GetValueConverter()`/`GetTypeMapping().Converter`/`GetBsonRepresentation()` — mirroring `NativeGroupByBinder.HasDefaultKeySerialization`). The value-converter/representation case is a correctness guard, not just an optimization gap: the driver-LINQ discriminator filter serializes the discriminator value RAW (`MongoEFDiscriminator.GetDiscriminatorsForTypeAndSubTypes` → `BsonValue.Create(GetDiscriminatorValue())`, bypassing the conversion), whereas the native predicate serializes it THROUGH the property serializer (applying the conversion), so an unguarded native `$eq`/`$in` would return a **different row set** than the established driver-LINQ path — violating `Native == DriverLinq`. The plain-string discriminators used by the default/shadow mapping modes have no converter/representation and stay native.
>
> **Projected `Distinct` (EF-347, SP6-remaining slice 2).** Native now also covers `Distinct()` applied to a terminal anonymous/DTO projection (`Select(new {...}).Distinct()`): `TranslateDistinct` calls `NativeGroupByBinder.TryBindDistinctFromProjection`, which converts each already-populated `Select.Projection` entry (alias → field-ref, from the SP3 `$project` pushdown) into a **degenerate `$group`** — a composite (or scalar) key made of those same field-refs, with **zero accumulators** — replacing `Projection` with a flattening projection that reads each value back out of `_id.<alias>` (`_id` for a single-part key), exactly mirroring the GroupBy flatten mechanism. `Route` resolves to `NativeRoute.GroupBy` (a grouping with no accumulators is still a grouping), so the lowerer's existing group branch emits `$group` → flatten `$project` with no special-casing. **No shaper changes**: the shaper was already built by the preceding `Select` to read the top-level result aliases by name, and those same aliases survive the flatten. `TryBindDistinctFromProjection` also sets a **separate** `Select.IsDistinct = true` provenance flag on success (NOT `IsGroupBy`) so the **existing** post-group guards (`NativeSlotPopulator`'s post-group slot-operator guard **and** `NativeCardinalityBinder.TryBindAggregate`/`TryBindReducer`, all now keyed on `IsGroupBy || IsDistinct`) also cover an operator applied **after** this `Distinct` — without it, e.g. a `Where` whose member name happens to match a real entity property name would resolve against the entity and get hoisted to a pre-group `$match` instead of being rejected. **Why a separate flag, not `IsGroupBy`:** the QMTEV `TranslateJoinCore` join-decline is grouping-semantics-specific — a genuine `GroupBy` joined via driver-LINQ returns silently-wrong (empty) joins, so it **hard-declines** (`MarkGroupByFallbackUnsafe` → throws under both `Native` and `NativeOnly`). A projected `Distinct` is just a flat row set the driver-LINQ path joins **correctly**, so `Distinct`-then-`Join` must fall back **gracefully** (`TranslateJoinCore` calls `MarkNotNativelyRepresentable` for an `IsDistinct`-and-not-`IsGroupBy` join side — runs correctly under `Native`/`DriverLinq`, throws only under `NativeOnly`). Reusing `IsGroupBy` for Distinct would have hard-thrown `Distinct().Join(...)` under `Native` — a correct-results→throw regression. Note the lowerer's group branch returns early after the `$group`+flatten `$project`, so a native `Distinct`+`Join` would silently drop the join — hence it must fall back (never go native), just gracefully. **Falls back to driver-LINQ:** a **bare-scalar** projection (`Select(o => o.Country).Distinct()` — bare-scalar `Select` never populates `Projection` in the first place, so `TryBindDistinctFromProjection`'s `Projection.Count == 0` guard rejects it) and a **whole-entity** `Distinct()` (same reason). **Rejected by a guard → fall back (correctness, not just an optimization gap):** a projected key backed by a property with a value converter or non-default `BsonRepresentation` — `HasDefaultKeySerialization` is reused as-is from the GroupBy binder (same generic-`_id`-readback divergence risk). `IsNativeRepresentableSlotOperator` gained `Distinct` so the slot-populator catch-all doesn't clobber `TranslateDistinct`'s own decision (mirroring how `Select`/`OfType` are listed). **A `GroupBy` applied AFTER a projected `Distinct` (`Select(new{..}).Distinct().GroupBy(k)`) falls back** via a dedicated guard at the top of `TranslateGroupBy`: because `GroupBy` has its own `Translate` override it bypasses the `IsGroupBy || IsDistinct` post-group guards in `NativeSlotPopulator`/`NativeCardinalityBinder`, so without this guard `TryBindGroupKey` would OVERWRITE the Distinct's key-only `Grouping` with the GroupBy's own key — silently DROPPING the Distinct and emitting a `$group` that counts ALL rows instead of distinct rows (a silent wrong-data bug). `TranslateGroupBy` therefore checks `IsDistinct || IsGroupBy || Grouping != null` first and, if set, marks the query non-native (and sets `IsGroupBy` for the join-decline machinery) WITHOUT rebinding — falling back to driver-LINQ. (The reverse, `Distinct` after `GroupBy`, is already safe: `TryBindDistinctFromProjection` returns `false` when `Grouping != null`.) **EF-347 slice 5 fix (whole-branch review, silent-null Distinct-after-SelectMany):** `TryBindDistinctFromProjection`'s guard also declines when `select.UnwindSource != null` — a projected `SelectMany` (owned or reference) is itself a terminal, and without this guard `Distinct` after one would convert the SelectMany's `Projection` into a degenerate `Grouping` while `UnwindSource` stayed set; `MongoSelectLowerer.Lower` checks `UnwindSource` BEFORE `Grouping` and returns early, so the `$group` this Distinct built was silently never emitted — the flatten `$project` read `_id.<alias>` fields that had nothing to read, producing `[null, null, null]` under BOTH `Native` and `NativeOnly` (the bug this guard fixes; see `NativeSelectManyTests.Reference_form_followed_by_Distinct_hard_fails_in_every_mode` / `Explicit_result_selector_form_followed_by_Distinct_falls_back_gracefully_except_under_NativeOnly`). This is the same family as the `Grouping != null` check on the same line — `TryBindDistinctFromProjection` now declines after ANY prior terminal (`Grouping` or `UnwindSource`), not just a prior `GroupBy`.
>
> **Include (EF-339, SP5 — flat collection Includes).** Native now covers **single-level collection `Include`** (`Include(x => x.Orders)`), plus a **projected collection `.Count`** (`Select(c => new { c.Orders.Count })` → server-side `$size` over the `$lookup` array). The lowerer emits a flat `$lookup` (no `$unwind`) after the canonical `$match → $sort → $skip → $limit` (so filter/sort/paging are pushed **ahead** of the join — the observable MQL change this sweep rebaselined); the DOM shaper then materializes the joined array. Collection `Include` is native via the **DOM** shaper (collection navs are ineligible for streaming, gated off by `StreamingEligibility`). **Reference `Include` remains deferred/fallback, unchanged by this sub-project** — it still nav-expands to a `LeftJoin` that the gate treats as non-native, so it falls back to driver-LINQ (correct results; throws under `NativeOnly`), exactly as before EF-339. Its native lookup/streaming machinery (lowerer `$lookup`/`$unwind`, `GetStreamingReferenceLookups`, streaming-Include eligibility) stays **built but dormant**, awaiting a future streaming sub-project. Native-vs-driver is decided by the lowerer (`TryBuildNativeFactory`) — collection Include goes native the moment the lowerer stops rejecting the flat `$lookup` shape, no separate gate flag. The `IsNativeCollectionLookup` discriminator (`As == LookupExpression.GetLookupAlias(Navigation)`, i.e. the un-prefixed `_lookup_<Nav>` alias) is what admits the flat single-level collection shape while still excluding the (still-fallback) reference-Include `_outer`/`_inner` shape and the collection-of-collection `_lookup_<Nav>._lookup_<Coll>` shape (which keeps prefixed aliases and stays fallback). **Still deferred/fallback:** reference `Include`, nested/transitive `ThenInclude`, filtered `Include`, collection-of-collection, and collection-array **streaming** materialization (SP5b / SP7). EF-334 later centralized the **is-native** gate decision into one `ClassifyNativeDisposition` classification (`Route` + vector-search + GroupBy-unsafe); `AllPendingLookupsAreStreamable` deliberately stayed a **separate** streaming-vs-DOM axis (it is not an is-native signal). The residual `$lookup`-state relocation off `MongoQueryExpression` is tracked under EF-317 (a driver-`LeftJoin`-gated removal), independent of EF-334.
>
> **GroupBy (EF-SP6 / EF-344).** Native now also covers **`GroupBy(key).Select(aggregate)`** → a server-side `$group` followed by a **flattening `$project`** that lifts the grouped output (the `_id` scalar key, each `_id.<Name>` composite sub-key, and each accumulator output field) up to top-level result aliases. The grouped rows materialize through the **existing DOM projection shaper** (`MongoProjectionBindingRemovingExpressionVisitor`) reading those flattened aliases by name — there is **no** bespoke grouping shaper and **no** `IGrouping` materializer (the gate compiles it DOM-only, `allowStreaming: false`, via the `NativeRoute.GroupBy` branch). **Keys:** a scalar key (`_id: "$field"`) or a composite/anonymous key (`_id: { Name: "$field", … }`), each part a plain field-ref. **Accumulators:** `Count`/`LongCount` → `$sum: 1` (including a *lone* `Count()` projection, `GroupBy(k).Select(g => new { g.Key, Count = g.Count() })` — this is **not** fused by EF into a `resultSelector`-bearing `GroupBy` and goes native); `Sum`/`Average`/`Min`/`Max` over a plain field-ref selector → `$sum`/`$avg`/`$min`/`$max`. **Mechanism:** `MongoGrouping` IR on `MongoSelectDefinition.Grouping` (with the transient `PendingGroupKey` holding the key while it is parsed ahead of the aggregate `Select`), `NativeRoute.GroupBy`, `NativeGroupByBinder` (`TryBindGroupKey` called from the QMTEV's `TranslateGroupBy` — which builds the grouped `ShapedQueryExpression` + `GroupByShaperExpression` **directly**, since the base `TranslateGroupBy` is abstract; `TryBindGroupProjection`/`TryBindAccumulator` called from the grouped-source branch of `TranslateSelect`), `MongoGroupStage` + `MongoPipelineFactory.RenderKeyedGroup`, and the `MongoSelectLowerer` group branch. The provider's own `EnumerableMethods` port was extended with the aggregate-selector members (EF Core's is `internal`).
>
> **Behavior change (not a break).** Before EF-344, `GroupBy` **hard-failed** translation (`NotTranslatedExpression`). Now supported shapes go native and **unsupported shapes fall back to driver-LINQ** (throwing only under `NativeOnly`), except the one declined family below. Per the versioning rubric this — and the emitted MQL — is **not** a breaking change: query results are unchanged; the change is hard-throw → native / graceful-fallback.
>
> **Falls back to driver-LINQ (correct results under default `Native`):** computed keys (`g => g.OrderDate.Year`) and other non-member key expressions that `TryBindGroupKey`'s switch rejects — it accepts only a plain `MemberExpression` or an anonymous-of-members `NewExpression`, so e.g. an `EF.Property<T>(o, "…")` key (a `MethodCallExpression`) falls back, computed accumulator operands (`g.Sum(x => x.A * x.B)`), **bare** `GroupBy(key)` / `GroupBy(key, elementSelector)` that terminate on the `IGrouping` sequence (`Route` stays `Fallback` while `PendingGroupKey` is set but `Grouping` is never finalized), a user-supplied `GroupBy(key, resultSelector)` (the QMTEV's `TranslateGroupBy` declines a non-null `resultSelector`), **post-group slot operators** (`Where`-as-HAVING / `OrderBy` / `OrderByDescending` / `ThenBy` / `ThenByDescending` / `Skip` / `Take` applied **after** the grouping) — these fall back **because of an explicit guard**, not by luck: `NativeSlotPopulator.PopulateNativeSlots` calls `MarkNotNativelyRepresentable()` and returns for these seven operators once `MongoSelectDefinition.IsGroupBy` is set (`IsPostGroupSlotOperator`), forcing a clean driver-LINQ fallback. Without the guard the post-group predicate/sort would be resolved against the **entity** type by member name (the translator is built from `CollectionExpression.EntityType`) and emit a **pre-`$group`** `$match`/`$sort` — and when an aggregate alias collides with a real property name (e.g. `Amount`) it silently returned wrong data. The guard is keyed on `IsGroupBy` (set unconditionally by `TranslateGroupBy`) rather than the finalized `Grouping`, and is scoped to the seven slot operators only so the supported `GroupBy(key).Select(aggregate)` (whose aggregate `Select` reaches `PopulateNativeSlots` with `IsGroupBy` already `true`) still goes native. **Post-group scalar aggregates and reducers** (`Count`/`LongCount`/`Sum`/`Min`/`Max`/`Average` / `First`/`Single`/`Any`/`All` applied **after** the grouping) fall back the same way, via a symmetric `IsGroupBy` guard at the top of BOTH `NativeCardinalityBinder.TryBindAggregate` **and** `NativeCardinalityBinder.TryBindReducer` (EF-344 pass-2). Without it, setting `Cardinality` on an already-grouped select flips `Route` to `ScalarAggregate` (which is prioritized above `GroupBy`) while the lowerer's grouping branch still emits `[$group, $project]` with no terminal `$count`/aggregate/limit stage — the scalar shaper then reads a nonexistent element and crashes with `KeyNotFoundException` instead of falling back. **Invariant: EVERY post-group / post-terminal operator entry point must gate on `IsGroupBy` (and, since EF-347, `IsDistinct`)** — currently `NativeSlotPopulator.PopulateNativeSlots` (the seven slot operators, on `IsGroupBy || IsDistinct`), `NativeCardinalityBinder.TryBindAggregate`/`TryBindReducer` (scalar aggregates / reducers, on `IsGroupBy || IsDistinct`), **`TranslateGroupBy`** (a `GroupBy` after ANY prior terminal — gates on the full `HasTerminalOperator`, captured as `hadTerminalGrouping` BEFORE the unconditional `IsGroupBy = true`, and falls back WITHOUT rebinding, else it overwrites the existing `Grouping`), **`TranslateSelectMany`** (a `SelectMany` after ANY prior terminal — gates on `HasTerminalOperator` at the top and returns `null` → clean hard-fail in every mode; see the SelectMany-after-terminal note below), and **`TranslateSelect`'s non-grouped projection branch** (a second projected `Select` after a native `Distinct`/`GroupBy` — gates on `Grouping != null || IsGroupBy || IsDistinct` and skips `TryPopulateNativeProjection`, else it appends a field-ref onto the terminal's `Projection` while `Grouping` is set → the lowerer emits a flatten `$project` over fields gone after the `$group` → silent nulls). `TryBindDistinctFromProjection` ITSELF is a fifth member of this family, though its own guard checks state directly rather than a flag: it always declined when `Grouping != null` (a prior `GroupBy`), and — since the EF-347 slice 5 whole-branch-review fix — also declines when `select.UnwindSource != null` (a prior projected `SelectMany`, owned or reference), i.e. `Distinct` now correctly declines after ANY prior terminal, not just a prior `GroupBy`; the missing `UnwindSource` check was the exact silent-null bug this fix closes (see the "Projected `Distinct`" note above). **EF-347 slice 4 adds a member to this family that runs the OPPOSITE way round — a check that must fire BEFORE the post-terminal guard, to correctly ADMIT a native shape rather than reject one:** `TranslateSelect`'s pending-SelectMany-projection branch, gated on `UnwindSource != null && Projection.Count == 0`, calls `NativeSelectManyBinder.TryBindTransparentIdentifierProjection` and — on success — returns early with the SelectMany's own result shaper, all before reaching the ordinary `IsTransparentIdentifierSelector`/`IsTransparentIdentifierMemberAccessSelector` post-terminal else-if chain below it. This ordering is load-bearing: a bare-nav owned `SelectMany` (`TryBindBareNavUnwind`, see the SelectMany note below) sets `UnwindSource`, which already satisfies `HasTerminalOperator` — so without binding the trailing projection FIRST, the ordinary post-terminal guard would reach this same `Select` first and wrongly call `MarkNotNativelyRepresentable()` on a shape that is, in fact, natively representable. **`Select`, `GroupBy`, `OfType`, and `SelectMany` are the own-`Translate`-override operators that need the explicit guard** (they bypass the `NativeSlotPopulator`/`NativeCardinalityBinder` catch-all guards) — `OfType` gates on `mongoQueryExpression.Select.HasTerminalOperator` at the top of its discriminator-predicate branch in `TranslateOfType`, added as a final-review fix once `Union`/`Concat` (EF-347 slice 2) made a whole-entity terminal reachable before `OfType` for the first time (previously `OfType` was only ever reached pre-terminal, so the gap was latent). Without it, `OfType` after a `Union`/`Concat` adds its discriminator conjunct to the OUTER select's tail `$match`, which the lowerer emits as a pre-`$unionWith` `$match` that filters only the outer rows — the `$unionWith` operand's own nested pipeline gets no discriminator filter, so sibling-type rows from the operand leak into the result (see `NativeSetOpsTests.OfType_after_union_falls_back`, which reproduces this as an `InvalidOperationException` at materialization — a leaked base-type document reaching a shaper fixed to the derived type — rather than a silently-wrong result, but the underlying leak is the same class of bug). `Distinct` is covered incidentally, not by an explicit `HasTerminalOperator` check: `TryBindDistinctFromProjection`'s `Projection.Count == 0` guard already rejects a whole-entity/post-terminal source, so it falls back for an unrelated reason that happens to also cover this case. **`SelectMany` also needs the explicit guard, and it was the one own-override operator that originally lacked it** (composition-seam audit finding): its binders (`NativeSelectManyBinder.TryBind*`) set `UnwindSource` on pure collection-selector STRUCTURE, with no post-terminal check, and `SelectManyWithCollectionSelector` is whitelisted in `IsNativeRepresentableSlotOperator` so the catch-all does not back it up either. Without the guard, `Union`/`Concat`-then-`SelectMany` (`Union(a,b).SelectMany(o => o.Items.Select(...))`) set `UnwindSource` alongside the already-attached `SetOperation`; because the lowerer checks `SetOperation` BEFORE `UnwindSource` (its fixed terminal precedence `SetOperation > UnwindSource > Grouping > Projection > Cardinality`), the `$unwind`/`$project` was SILENTLY DROPPED — the query returned whole outer union rows (wrong row count, or a shaper crash when a projected alias is absent at top level) under BOTH `Native` and `NativeOnly` (`Route` stayed non-`Fallback`, so `NativeOnly` did not even throw). The fix gates the 2-arg `TranslateSelectMany` on `HasTerminalOperator` at the top and returns `null` (not `MarkNotNativelyRepresentable()`): a graceful fallback is NOT viable here because the native SelectMany builds a by-index `ProjectionBindingExpression` shaper the driver-LINQ fallback cannot re-read — the SAME shaper-rebuild limitation that makes operators composed AFTER a SelectMany hard-fail in every mode — so `null` (EF's own translation-failure path, a clean hard-fail in every mode) is the correct, contract-consistent decline. See `NativeCompositionSeamAuditTests`. (A related latent-but-currently-unreachable gap: `IsPlainWholeEntitySelect` — the set-op operand guard — does not check `UnwindSource`, so `Union` AFTER a bare-nav `SelectMany` is not explicitly rejected; unreachable today because the trailing projected `Select` sets `Projection` → `Route = Projection` first, which `IsPlainWholeEntitySelect`'s `Route == WholeEntity` check already rejects.) `Cast` needs no guard: `TranslateCast` is a pure shaper reshape (`ReshapeShaperExpression`) that mutates no outer slot or predicate. A future own-override operator applied after a terminal must add the same gate. (Empirical note for the `TranslateSelect` case: in the current provider this shape additionally throws at shaper build — `MongoProjectionBindingExpressionVisitor` cannot read a prior anonymous projection's members — in *all* modes, so the null-data symptom is not presently reachable; the guard is defense-in-depth for a future shaper-builder that makes chained-projection shapers buildable.) Any future reducer/aggregate/operator added after a group must add the same guard, or it reopens this class of bug (the post-group operator resolves against the base entity / picks the wrong route). **EF-347 slice 3 adds a further entry point to this family — a guard against a wrong REJECTION rather than a wrong RESULT:** `Visitors/MongoProjectionBindingExpressionVisitor`'s `ProjectionBindingExpression { Index: not null }` case in `Visit` passes an already-resolved-by-index projection member straight through, gated on `_queryExpression.Select.Route == NativeRoute.Projection`. It exists specifically so EF's mandatory unwrap-`Select` (`.Select(ti => ti.Inner)`, always synthesized right after a native SelectMany's `TransparentIdentifier(Outer, Inner)` result-selector — see the SelectMany note above) can re-enter the shaper this visitor already built for the SelectMany's own projection, instead of falling into the ordinary `MemberExpression` re-binding path (which would try to derive a brand-new projection mapping for a member access that no longer resolves against any real CLR type) — a case the other post-terminal guards in this family never had to handle, because none of them re-enter this visitor a second time on the same already-bound expression. The `Route == Projection` gate is deliberately narrower than `HasTerminalOperator`: `GroupBy`/`Distinct` flip `Route` to `Fallback` (via `MarkNotNativelyRepresentable()`) BEFORE this visitor ever runs for a projected `Select` applied after either of them, so that case still correctly falls through to the translate-failed throw instead of being passed through here — passing through unconditionally would silently defeat the established `TranslateSelect`/`TranslateGroupBy` guards documented above (see the load-bearing comment at the case itself, which also cross-references the `StructuralTypeShaperExpression` precedent this mirrors). Also falling back: the broader long tail the driver-LINQ provider already handles (`Distinct`/`Union`/join-then-group/nav-key groupings) — all reachable now precisely because `GroupBy` no longer hard-throws.
>
> **Rejected by guards → fall back (the guards PREVENT native wrong-results):** a grouping key with a value converter or a non-default `BsonRepresentation` (`NativeGroupByBinder.HasDefaultKeySerialization`) — the generic `_id` readback (the flattened `_id` has no backing `IProperty`) would diverge from the property's configured serializer; and an accumulator whose source is **not** the grouping parameter `g` (a correlated cross-collection subquery, a navigation, a different collection) (`NativeGroupByBinder.IsGroupingSource`) — binding it to a `$group` accumulator would silently drop the subquery and collapse e.g. a subquery `Count` to `$sum:1`.
>
> **Declines to a clean `NativeTranslationNotSupportedException` — NO fallback, under both `Native` and `NativeOnly`:** a **group-first** `GroupBy` combined with a `Join`/`GroupJoin`/`LeftJoin` (a join taken over a grouped source, e.g. the spec's `GroupBy_Aggregate_Join`). Its driver-LINQ fallback returns **silently wrong** data (the joined entity is empty for every group), so the provider declines rather than route to a known-wrong result: `MongoSelectDefinition.MarkGroupByFallbackUnsafe()` (set in the QMTEV `TranslateJoinCore` when either join side `IsGroupBy`) trips a dedicated gate check in `MongoShapedQueryCompilingExpressionVisitor.VisitShapedQuery`. **Explicit `MongoQueryMode.DriverLinq` still executes it** (the user's opt-in, carrying the same wrong-data caveat). **Join-then-group** (an aggregate over a join result) falls back **correctly** and is unaffected — do not conflate the two orderings.
>
> **Set operations — `Union`/`Concat` (EF-347 slice 2).** Native now also covers a **whole-entity, terminal** `Union`/`Concat` over the SAME entity type: `TryTranslateSetOperation` (backing both `TranslateUnion` and `TranslateConcat`) attaches a `MongoSetOperation` (`Kind` + the second operand's own `MongoSelectDefinition` + its collection name) to source1's `Select.SetOperation` when both operands are a plain whole-entity select (`IsPlainWholeEntitySelect` — filter/sort/paging only, no projection/grouping/cardinality/lookups/`VectorSearch`/own set-op) of the matching `CollectionExpression.EntityType`; otherwise it calls `MarkNotNativelyRepresentable()` and returns source1 unchanged — `TryTranslateSetOperation` **always** returns non-null (mirroring `TranslateGroupBy`'s contract), so an out-of-scope shape falls back gracefully rather than hard-failing translation. The lowerer (`MongoSelectLowerer.Lower`) renders the attached `SetOperation` as a terminal `MongoUnionWithStage` — the operand's own canonical `$match`/`$sort`/`$skip`/`$limit` stages, lowered independently via the same `AppendCanonicalStages` helper the outer query uses — appended **last**, after the outer query's own canonical stages and any `$lookup`. `MongoPipelineFactory.RenderUnionWith` renders the operand's nested pipeline into the aggregation `$unionWith` stage using the **same shared `PlaceholderTable`** as the outer query, so a parameter captured in the operand's own predicate (e.g. `set1.Union(set2.Where(c => c.X == capturedLocal))`) is substituted correctly at `Build(parameterValues)` time. `Union` additionally emits a `$group{_id:"$$ROOT"}` + `$replaceRoot` dedup stage after the `$unionWith` (full-document de-duplication, matching BCL `Union` set semantics); `Concat` does not. **`IsSetOp` joins `HasTerminalOperator`** (`IsGroupBy || IsDistinct || IsSetOp || Grouping != null`) — the same centralized post-terminal gate `GroupBy`/`Distinct` use. **Invariant: set operations are terminal-only** — every post-terminal entry point (`NativeSlotPopulator`'s seven slot operators, `NativeCardinalityBinder`'s aggregates/reducers, `TranslateGroupBy`'s rebind guard, and `TranslateSelect`'s two guards — the hoisted-shared-Include guard and the non-grouped-projection guard) already gated on `HasTerminalOperator` before this slice, so `Union`/`Concat` need **no new guard code** to be covered — they only need `IsSetOp` folded into the shared predicate, which is exactly what `HasTerminalOperator` does. **EF-347 slice B relaxed this at exactly the two CATCH-ALL guard sites** — `NativeSlotPopulator`'s post-group/post-terminal slot guard and `NativeCardinalityBinder.TryBindReducer`/`TryBindAggregate` — both now check `HasTerminalOperator && !IsSetOpTerminalOnly` instead of `HasTerminalOperator` alone. `IsSetOpTerminalOnly` (`MongoSelectDefinition`) is `IsSetOp && !IsGroupBy && !IsDistinct && Grouping == null && UnwindSource == null` — narrowly true only when a set op is the SOLE terminal seen so far. With the relaxation, `Where`/`OrderBy`/`ThenBy`/`Skip`/`Take` and scalar aggregates/reducers (`Count`/`Sum`/`Min`/`Max`/`Average`/`First`/`Single`/…) composed AFTER a set op fall through to their ordinary arms and record into a **second** ordered op list, `MongoSelectDefinition.TrailingOps` (mirroring `PipelineOps`), via the same five merge methods (`AddPredicateConjunct`/`StartOrReplaceSort`/`AppendThenBy`/`AppendSkip`/`AppendLimit`) — those methods target a private `ActiveOps` property that is `PipelineOps` normally but flips to `TrailingOps` the instant `SetOperation` is attached, so the flip point is single and well-defined. `MongoSelectLowerer.Lower` emits `PipelineOps` (source1's pre-terminal ops), then the set-op stage (`$unionWith`/`$group`/`$replaceRoot` for `Union`, or the source-tagging pipeline for `Intersect`/`Except` — see the slice A note below), then `TrailingOps` verbatim, then falls through to the ordinary cardinality-stage tail — so a trailing filter/sort/page or a terminal reducer/aggregate now operates on the COMBINED set-op result, not source1's pre-set-op rows (see `NativeSetOpsTests.Where_after_union_goes_native`/`OrderBy_after_union_goes_native`/`Paging_after_union_goes_native`/`Count_after_union_goes_native`/`First_after_union_ordered_goes_native`). **`IsSetOpTerminalOnly` relaxes ONLY those two catch-all sites** — every deferred own-`Translate`-override operator (`Select`/`Distinct`/`GroupBy`/`SelectMany`/`OfType`) and a chained set op keep their OWN untouched `HasTerminalOperator` check (no `!IsSetOpTerminalOnly` escape hatch anywhere else), so they remain terminal after a set op exactly as before slice B — the relaxation surface is deliberately these two sites and nowhere else, since every other post-terminal entry point already decides representability itself via its own guard. A **chained** `set1.Union(set2).Union(set3)` therefore still falls back gracefully, unaffected by slice B: `IsPlainWholeEntitySelect` rejects source1 once its `Select.IsSetOp` is `true`, so the second `Union` marks it non-native rather than nesting `$unionWith` stages — chaining is declined in `TryTranslateSetOperation`'s own scope gate, not one of the two relaxed catch-alls. **`Intersect`/`Except` now also go native for the same whole-entity, terminal shape — and share the identical slice-B trailing-ops relaxation — via a different rendering and a different guard-decline path — see the set-ops slice A note immediately below.** **EF-347 slice C2: a trailing anonymous/DTO member-access `Select` after a whole-entity set op now ALSO goes native** — `Union(A,B).Select(x => new {...})` emits a `$project` after the set-op stage, via the SAME slice-B `TrailingOps`/`ActiveOps` fall-through plus the pre-existing SP3 projection binder (`NativeProjectionBinder.TryPopulateNativeProjection`) — no lowerer or shaper change was needed, since the lowerer already appends `Projection` last and the SP3 shaper is set-op-agnostic. **Mechanism (seam closure):** `IsSetOpTerminalOnly` gained a `Projection.Count == 0` conjunct — this ADMITS the trailing projection (`Projection` is still empty at the moment `TranslateSelect`'s post-terminal guard runs, since a set-op-only terminal with nothing else attached is still "terminal-only") and, symmetrically, CLOSES post-projection composition: once `Projection` is populated, `IsSetOpTerminalOnly` flips to `false`, so a `Where`/aggregate/second `Select` applied AFTER the trailing projection no longer qualifies for the two catch-all-relaxation sites and falls back (Union/Concat) or hard-fails (Intersect/Except) exactly like any other post-terminal operator. **Empirical finding (Task 2, verified via captured MQL, not assumed):** for a predicate/selector that is a PURE, invertible pass-through of the trailing projection's own member(s) (e.g. `.Select(i => new{N=i.Value}).Where(x=>x.N>=2)`, or two consecutive member-access Selects), EF Core's own query compiler (the `NavigationExpandingExpressionVisitor` pending-selector mechanism) pushes/fuses the composition BEFORE the Select ever reaches this provider's translator — so these two specific shapes never actually exercise the post-projection-composition seam at all; they translate as an ordinary trailing-`Where`-then-trailing-`Select` (or a single fused Select) and go fully native with correct results. This is a general EF Core LINQ-compilation behavior, not something this provider controls or something specific to set operations — the same fusion is observed for a plain aggregate (`Sum(x => x.N)`) too (see `NativeSetOpsTests.Where_after_trailing_projection_on_union_goes_native_via_ef_predicate_pushdown`/`Second_projection_after_union_goes_native_because_ef_fuses_the_two_selects` — named for the observed behavior, not the originally-assumed fallback — and the corresponding Intersect variants). **Settled finding (an exhaustive follow-up probe, recorded here so it is not re-litigated):** no public-LINQ shape has been found that actually reaches the `Projection.Count == 0` seam. The probe tried seven post-projection operators — `Distinct`, `GroupBy`, a chained set op, `Skip`, `Take`, `Count`, `First` — against BOTH sites `IsSetOpTerminalOnly` actually gates (`NativeSlotPopulator`'s slot operators, `NativeCardinalityBinder`'s aggregates/reducers): every lambda-bearing or parameterless per-row operator (`Skip`/`Take`/`Count()` included) gets the identical EF pending-selector pushdown described above before the translator ever sees post-projection composition, and every operator that genuinely forces materialization of the projected row (`GroupBy`, a chained set op) is declined by its OWN independent `IsSetOp`/`HasTerminalOperator` guard, unconditional on `IsSetOpTerminalOnly` — while `Distinct` in that same position is bound natively (see the reconciled trailing-`Distinct` note below), again without ever consulting `IsSetOpTerminalOnly`. So the `Projection.Count == 0` conjunct's seam-closure half is **defensive-only** — it hardens `IsSetOpTerminalOnly` against a hypothetical future internal composition path, not against anything reachable today via ordinary LINQ — while its **essential** half (admitting the trailing projection at push-down, described above) is the one every functional test in this file actually exercises. The seam-closure half's sole proof, and the one expected to remain its sole proof, is the IR-level unit test `MongoSelectDefinitionTrailingOpsTests.IsSetOpTerminalOnly_false_when_a_projection_is_populated`. **Falls back to driver-LINQ (correct results under default `Native`):** a **bare-scalar, computed-leaf, or entity-ref** set-op OPERAND (`Select(a => a.Name).Union(...)`, `Select(a => new { X = a.Value * 2 }).Union(...)`) — SP3 never pushes these down, so the operand is not a plain projected select (contrast the SUPPORTED anonymous/DTO member-access projected-**OPERAND** shape — a DIFFERENT thing from the slice-C2 trailing projection above — which now goes native; see the dedicated projected-operand set-ops note below), a **mixed** whole-entity/projected operand pair (one operand whole-entity, the other projected — fails both scope gates), an operand carrying an `Include` (EF Core itself hoists a shared `Include` to apply AFTER the combinator, reaching `TranslateSelect` as `Union(A,B).Select(x => Include(x))` — caught by its `HasTerminalOperator` guard, untouched by slice B), mismatched operand entity types (defensive; unreachable in practice because EF Core's own `NavigationExpandingExpressionVisitor` rejects incompatible set-operation sources upstream), and — still deferred as of slice C2 — a **bare-scalar** trailing projection (`Select(i => i.Value)` — SP3 never pushes a bare scalar, set-ops or not), a **computed-leaf** trailing projection (arithmetic/method calls inside the projection body), an **entity-ref** trailing projection, a hypothetical genuine **post-projection composition** (a `Where`/aggregate/second `Select` whose predicate/selector is NOT a pure pass-through of the projection) — the shape the `Projection.Count == 0` conjunct is designed to decline, though (per the settled finding above) no such shape has been shown reachable via ordinary LINQ composition — a **bare `Distinct`** applied directly to a whole-entity set-op result with NO trailing projection (`Union(A,B).Distinct()` — `TryBindDistinctFromProjection`'s own `Projection.Count == 0` check rejects this, confirmed empirically), a **chained** set op, and **`GroupBy`/`OfType`/`SelectMany` composed after a set op** (each still trips its own untouched `HasTerminalOperator` guard and falls back gracefully — throws only under `NativeOnly` — since Union/Concat retain their driver-LINQ baseline; see `NativeSetOpsTests.Bare_scalar_projection_after_union_falls_back_gracefully`/`Computed_leaf_projection_after_union_falls_back_gracefully`/`Chained_union_falls_back`/`GroupBy_after_union_falls_back`/`OfType_after_union_falls_back`). **A `Distinct` applied AFTER the slice-C2 trailing projection instead goes NATIVE with correct results** — confirmed empirically (`Union(A,B).Select(i=>new{N=i.Value}).Distinct()` under `NativeOnly` succeeds, `[1,2,3,4,5]`): `TryBindDistinctFromProjection` (`NativeGroupByBinder`) converts whatever `Projection` is already populated into a degenerate `$group`, unconditional on `IsSetOp`/`IsSetOpTerminalOnly`, so it "just works" for a set op's trailing projection exactly as for any other terminal projection — a pre-existing, `IsSetOpTerminalOnly`-independent capability, not something slice C2 itself added. **Deferred (out of scope, unchanged by slice C2):** a bare-scalar/computed-leaf/entity-ref set-op OPERAND, or a mixed whole-entity/projected operand pair (both just described above — a projected/DTO-member-access OPERAND pair now goes native, see the dedicated slice C1 note below) — and `SelectMany` combined with a set op — neither is a gap discovered later, both are the intentional remaining terminal-only scope.
>
> **Set operations — `Intersect`/`Except`, source-tagging (EF-347, set-ops slice A).** Native now also covers a **whole-entity, terminal** `Intersect`/`Except` over the SAME entity type, reusing the SAME `TryTranslateSetOperation`/`IsPlainWholeEntitySelect` scope gate as `Union`/`Concat` above — but MongoDB has no direct intersect/except pipeline stage, so the lowerer renders a different, **source-tagging** MQL shape instead of a plain `$unionWith`, and the guard-decline path (below) also differs. **Mechanism:** both operands are the SAME collection (entity type ⇒ one collection), so documents on both sides have identical shape, making full-document `$$ROOT` value-equality well-defined — the same assumption `Union`'s own dedup already relies on. `MongoSelectLowerer.Lower` renders the attached `SetOperation` as a `MongoSetDifferenceStage` (instead of `MongoUnionWithStage`) for these two kinds, and `MongoPipelineFactory.RenderSetDifference` renders: dedup the outer operand (`$group{_id:"$$ROOT"}`) then tag it (`$project` with `_id:0`, the real document moved to `_doc`, plus literal `_a:true, _b:false`); render the inner operand into the SAME shared `PlaceholderTable` (so a captured parameter still substitutes correctly), dedup and tag it `_a:false, _b:true`; `$unionWith` the two; re-unify by full document (`$group{_id:"$_doc", _a:{$max:"$_a"}, _b:{$max:"$_b"}}` — `$max` over a boolean collapses to "present on that side"); discriminate with a final `$match`; then `$replaceRoot{newRoot:"$_id"}` to restore the plain document. `_a`/`_b` are siblings of `_doc`, never real entity fields, so there is no field-name-collision risk (verified by `NativeSetOpsTests.Intersect_with_real_element_named_underscore_a_is_not_corrupted_by_the_source_tag`). **`Intersect` vs `Except` differ ONLY in the final `$match`:** `{_a:true, _b:true}` (present on both sides) vs `{_a:true, _b:false}` (present in the first operand only) — every stage upstream of that `$match` is identical between the two kinds. **Result order is not guaranteed** — the re-unifying `$group` stages reorder rows, matching a database set operation and matching `Union`'s already-documented lack of an ordering guarantee. **No driver-LINQ baseline, unlike `Union`/`Concat`:** `TryTranslateSetOperation`'s guard-decline branch is kind-conditional — for `Union`/`Concat` an out-of-scope shape still calls `MarkNotNativelyRepresentable()` and returns `source1` (the graceful fallback documented above), but for `Intersect`/`Except` it returns `null` instead, because the C# driver's own LINQ v3 provider does not translate a cross-view `Intersect`/`Except` either (confirmed empirically by a throwaway probe test before this slice was built). `null` reaches EF Core's own `NotTranslatedExpression` failure path directly — a clean hard-fail in **every** `MongoQueryMode` (`Native`/`DriverLinq`/`NativeOnly`) for an out-of-scope set-op shape. This mirrors reference `SelectMany`'s no-baseline decline (EF-347 slice 5): a `null` return, not a graceful `MarkNotNativelyRepresentable()`, is the correct decline whenever there is no working driver-LINQ path to land on. **EF-347 slice B: `Where`/`OrderBy`/`ThenBy`/`Skip`/`Take` and scalar aggregates/reducers composed AFTER a native `Intersect`/`Except` now go NATIVE too**, via the identical `TrailingOps`/`ActiveOps`/`IsSetOpTerminalOnly` relaxation documented in the `Union`/`Concat` note above — both set-op kinds share the one `SetOperation`/`IsSetOp` mechanism, so the relaxation at the two catch-all guard sites (`NativeSlotPopulator`, `NativeCardinalityBinder.TryBindReducer`/`TryBindAggregate`) is kind-agnostic. E.g. `{1,2,3}.Except({3,4,5})` then `.Where(Value >= 2)`, or `.Intersect(...).OrderBy(...).Take(n)`, or `.Intersect(...).Count()` now all emit the trailing `$match`/`$sort`/`$skip`/`$limit`/aggregate stage AFTER the source-tagging pipeline above, over the COMBINED Intersect/Except result — see `NativeSetOpsTests.Except_then_Where_goes_native`/`Intersect_then_paging_goes_native`/`Count_after_intersect_goes_native`. **EF-347 slice C2: a trailing anonymous/DTO member-access `Select` after a whole-entity Intersect/Except now ALSO goes native**, exactly as documented for Union/Concat immediately above — same `Projection.Count == 0` seam-closure mechanism, same `NativeProjectionBinder` reuse, no lowerer/shaper change. Because Intersect/Except still have no driver-LINQ baseline at all, an out-of-scope projection shape (bare-scalar, computed-leaf, entity-ref) or genuine post-projection composition still hard-fails in EVERY mode via the SAME `MarkNotNativelyRepresentable()` → gate → driver-throws path described below (see `NativeSetOpsTests.Select_after_intersect_goes_native_result_set`/`Where_after_trailing_projection_on_intersect_goes_native_via_ef_predicate_pushdown`). **An operator that stays DEFERRED — a bare `Distinct` applied directly to a whole-entity Intersect/Except result with NO trailing projection, a chained set op, `GroupBy`/`OfType`/`SelectMany` composed after Intersect/Except, or a hypothetical genuine post-projection composition (a `Where`/aggregate/second `Select` whose predicate/selector is NOT a pure pass-through of the trailing projection — per the settled finding in the Union/Concat note above, no such shape is known reachable via ordinary LINQ, so this remains a defensive-only decline path) — still trips the untouched `HasTerminalOperator` guard on its own `Translate*` override (the relaxation above touches only the two catch-all sites, nowhere else), but — unlike `Union`/`Concat`'s deferred set — hard-fails in EVERY mode rather than falling back gracefully**, because there is still no driver-LINQ baseline for `Intersect`/`Except` at all: the guard calls `MarkNotNativelyRepresentable()` → the gate builds the driver-LINQ fallback from the captured chain → the driver then throws because it cannot translate the `Intersect`/`Except` in that chain, in `Native` and `DriverLinq` alike (`NativeOnly` throws earlier, at the gate itself, before the fallback is even attempted). **A `Distinct` applied AFTER the trailing projection, unlike the bare case, goes NATIVE here too** — confirmed empirically (`Intersect(A,B).Select(i=>new{N=i.Value}).Distinct()` under `NativeOnly` succeeds, `[3]`; same query under default `Native` also succeeds with the identical result) — via the same `IsSetOp`-agnostic `TryBindDistinctFromProjection` mechanism documented in the Union/Concat note above. (The one exception that DOES take the `null` path is a `SelectMany` composed after the set op, since `TranslateSelectMany`-after-a-terminal returns `null` rather than marking non-native — see the SelectMany-after-terminal note above.) Either way the observable outcome for a deferred operator is identical — throws, never wrong data — exactly like `Union`/`Concat`'s own deferred set (see `NativeSetOpsTests.Where_after_trailing_projection_on_intersect_still_hard_fails_under_explicit_DriverLinq`/`Projected_intersect_hard_fails_in_every_mode`/`Intersect_then_op_hard_fails_under_native`). **New IR/stage:** `MongoSetOperationKind.Intersect`/`.Except` (`Expressions/MongoSetOperation.cs`); `MongoSetDifferenceStage` (`NativeTranslation/Stages/MongoSetDifferenceStage.cs`) → `MongoPipelineFactory.RenderSetDifference`; both are added to `IsNativeRepresentableSlotOperator` alongside `Union`/`Concat`, for the same catch-all-suppression reason. **A→B→C decomposition status:** this note describes slice A (the whole-entity-terminal foundation) plus the slice-B trailing-ops relaxation and the slice-C2 trailing-projection relaxation, both folded in immediately above. **Post-set-op composition for all four set operators, including a retrofitted `Union`/`Concat`, is DONE (slice B)** — in scope: `Where`/`OrderBy`/`ThenBy`/`Skip`/`Take`, scalar aggregates, `First`/`Single` reducers. **A trailing anonymous/DTO member-access projection, for all four set operators, is DONE (slice C2)** — still deferred: bare-scalar/computed-leaf/entity-ref trailing projections, a hypothetical genuine post-projection composition (a `Where`/aggregate/second `Select` that EF does not itself fuse past the projection — not shown reachable via ordinary LINQ, per the settled finding above), a **bare** `Distinct` with no trailing projection, a chained set op, and `GroupBy`/`OfType`/`SelectMany` composed after a set op. (A `Distinct` applied AFTER the trailing projection is instead a demonstrated native capability, for all four set operators — see above; it was never gated by `IsSetOpTerminalOnly` in the first place.) **Projected-operand set operations, for all four operators, are DONE (slice C1)** — see the dedicated note immediately below. With slice C1 complete, the set-ops decomposition (A → B → C1/C2) is now COMPLETE.
>
> **Set operations — projected operands (EF-347 slice C1, the FINAL set-ops slice).** Native now also covers a set operation whose OPERANDS are themselves a terminal SP3 anonymous/DTO member-access projection — `A.Select(new{...}).Union(B.Select(new{...}))` and the `Concat`/`Intersect`/`Except` equivalents — distinct from slice C2's trailing projection (`Union(A,B).Select(new{...})`), which projects the COMBINED result AFTER the set op rather than each operand BEFORE it. The two operands may be the SAME or DIFFERENT collections/entity types, provided their projected shapes (top-level alias sets) match. **Gate:** `TryTranslateSetOperation` adds a second acceptance branch alongside the existing whole-entity one — `IsPlainProjectedSelect(mongo)` (the projected analogue of `IsPlainWholeEntitySelect`: `Route == NativeRoute.Projection && Projection.Count > 0`, no grouping/cardinality/own-set-op/SelectMany/lookups/join/`VectorSearch`) on BOTH operands, plus `ProjectionShapesMatch` (equal top-level alias SETS between the two operands' `MongoProjection`s — same count, same alias names) — a CORRECTNESS guard, not just an optimization, since the dedup/source-tagging stages below compare whole projected documents by value and a mismatched alias set would compare structurally-different documents. The `CollectionExpression.EntityType`-equality check the whole-entity path uses does NOT apply on this path — that is exactly what admits different-collection operands (EF Core's own `NavigationExpandingExpressionVisitor` already rejects operands with incompatible shapes upstream, so `ProjectionShapesMatch` is defense-in-depth, not the primary safety net). On acceptance, `MongoSetOperation` gets a new `OperandsProjected` (`bool`) provenance flag, set `true`. **Lowerer:** `MongoSelectLowerer.Lower`, when `OperandsProjected`, emits source1's `$project` (`select.Projection`) immediately BEFORE the set-op stage (ahead of `$unionWith`/the set-difference pipeline) and the operand's own `$project` (`setOp.OperandSelect.Projection`) as the LAST stage of the nested operand pipeline — then suppresses the ordinary trailing-`Projection` fall-through (stage 6) so source1's projection is not double-emitted. `Union`'s dedup (`$group{_id:"$$ROOT"}`) and `Intersect`/`Except`'s source-tagging pipeline are UNCHANGED — they still operate on `$$ROOT`, which is now a projected document rather than a whole entity. **Dedup semantics — deliberately DIFFERENT from C2:** C1 `Union` dedups over the PROJECTED values (two distinct entities that project to equal values collapse to one row), matching BCL `A.Select(p).Union(B.Select(p))`; C2 dedups whole entities BEFORE projecting (see `NativeSetOpsTests.Projected_operand_union_dedups_over_projected_values_not_whole_entities`). **Different-collection operands have NO driver-LINQ oracle** — the provider's own cross-DbSet guard in the driver-LINQ fallback bridge (`MongoEFToLinqTranslatingExpressionVisitor`) throws for two different entity roots, so a different-collection projected-operand query is native-only by construction; proven via `NativeOnly` plus an expected-result-set assertion rather than `Native == DriverLinq` parity (same-collection operands DO have a driver oracle and ARE asserted for parity — see `NativeSetOpsTests.Different_collection_projected_operand_union_goes_native`/`Projected_operand_union_with_per_operand_filter_and_parameter_goes_native`). **Terminal-only, same as every other set-op shape:** because `Projection` is already populated at attach time, `IsSetOpTerminalOnly` (which requires `Projection.Count == 0`) is `false` from the start, so **no new guard code** is needed to close post-composition — every existing post-terminal entry point declines it automatically. For `Union`/`Concat` (same collection) this is a GRACEFUL fallback (correct results under `Native`, throws only under `NativeOnly`); `Intersect`/`Except` hard-fail in every mode (no oracle) — the same pattern as slice B. **Exception — a crash, not a graceful fallback:** a trailing SECOND `Select` (`A.Select(p).Union(B.Select(p)).Select(x => ...)`) crashes with a raw `InvalidOperationException` ("The LINQ expression 'ProjectionBindingExpression: Value' could not be translated") in EVERY mode, including explicit `DriverLinq` — a `ReplacingExpressionVisitor` `NewExpression`-fold reaches a `ProjectionBindingExpression` leaf that `MongoProjectionBindingExpressionVisitor` cannot read. This is an unsupported shape that never worked via driver-LINQ either (confirmed empirically: the identical exception fires under explicit `DriverLinq`, bypassing native translation entirely) — not a regression, and per the versioning rubric the exception TYPE of an unsupported shape is not part of the contract (see `NativeSetOpsTests.Second_select_after_projected_operand_union_hard_fails_in_every_mode`). **The `Distinct`-decline (regression fix):** `NativeGroupByBinder.TryBindDistinctFromProjection` declines — `select.SetOperation is { OperandsProjected: true }` — when a trailing `Distinct` is applied directly atop a projected-operand set op, because for THIS shape `select.Projection` is operand-1's OWN pre-combine projection (needed by the lowerer above), not a trailing post-combine projection; converting it into a degenerate `$group` in place would corrupt operand-1's pipeline (this was the original regression — a malformed pipeline that crashed at BSON deserialization — fixed by narrowing the decline to `OperandsProjected: true` specifically). So `A.Select(p).Union(B.Select(p)).Distinct()` now falls back gracefully for `Union`/`Concat`, and hard-fails every mode for `Intersect`/`Except` (no oracle), exactly like any other post-composition operator — see `NativeSetOpsTests.Distinct_after_projected_operand_union_falls_back_gracefully`/`Distinct_after_projected_operand_concat_falls_back_gracefully`/`Distinct_after_projected_operand_intersect_hard_fails_in_every_mode`/`Distinct_after_projected_operand_except_hard_fails_in_every_mode`. The guard is deliberately narrowed to `OperandsProjected: true` rather than a blanket `SetOperation != null`, to PRESERVE the pre-existing, unrelated C2 native capability: `Union(A,B).Select(p).Distinct()` (`OperandsProjected == false` — a genuine trailing projection over the combined result) still converts to a native `$group` exactly as before slice C1 (see `NativeSetOpsTests.Trailing_distinct_after_whole_entity_union_still_goes_native`). **Deferred, unchanged:** bare-scalar/computed-leaf/entity-ref operands and a mixed whole-entity/projected operand pair (see the Union/Concat and Intersect/Except notes above), and post-composition generally (auto-closed, as described above). **New IR, no new stage:** `MongoSetOperation.OperandsProjected` (`bool`, `Expressions/MongoSetOperation.cs`); new predicates `IsPlainProjectedSelect`/`ProjectionShapesMatch` (`Visitors/MongoQueryableMethodTranslatingExpressionVisitor.cs`) — the existing `MongoUnionWithStage`/`MongoSetDifferenceStage` are reused unchanged; only the `$project` PLACEMENT changes. **With slice C1 done, the set-ops decomposition is COMPLETE: A (whole-entity terminal, `Intersect`/`Except`) → B (post-composition, all four) → C1 (projected operands, all four) / C2 (trailing projection, all four).**
>
> **Owned-collection `SelectMany` — both user-authored forms (EF-347 slices 3–4).** Native now covers owned-collection `SelectMany` projecting the collection element's members, as a `$unwind` + `$project`, in BOTH shapes EF's nav-expansion can produce. **Inner-`Select` form (slice 3):** `q.SelectMany(o => o.Items.Select(i => new {o.X, i.Y}))` — the projection is nested inside the collection selector itself. **Explicit-result-selector / query-syntax form (slice 4):** `SelectMany(o => o.Items, (o, i) => new {o.X, i.Y})` / `from o in q from i in o.Items select new {o.X, i.Y}` — nav-expansion normalizes BOTH of these user spellings to a BARE owned-nav collection selector (no nested `Select`) plus a SEPARATE trailing `Select` over the `TransparentIdentifier(Outer, Inner)` result. **This remains ADDITIVE, not a behavior change to an existing contract:** before slice 3, `SelectMany` hard-failed translation **unconditionally, in every `MongoQueryMode`** (no driver-LINQ fallback attempt at all — the QMTEV's `TranslateSelectMany` override simply returned `null`, reaching EF Core's own translation-failure path directly); each slice can only ever add newly-native shapes, never regress a previously-working one (the one behavior REFINEMENT, not a regression, is the whole-inner-entity clean decline below, which replaces an internal crash with a clean exception — the shape was never supported either way). **Mechanism, inner-`Select` form:** `TranslateSelectMany` calls `NativeSelectManyBinder.TryBind(mongoQueryExpression, collectionSelector)`, which — for the accepted shape only — sets `MongoSelectDefinition.UnwindSource` (the owned nav's element path) and populates `Projection` via the **two-scope** binder: outer (closed-over) members translate against `CollectionExpression.EntityType` to a root field ref (`$X`); inner (the nested `Select`'s own parameter) members translate against the owned nav's target entity type and get the unwind path prefixed (`$Items.Y`) — the two translators are kept **structurally separate** (not just parameterized) so a member name shared between the outer and inner types (e.g. both having a `Name`) can never resolve against the wrong scope (see `NativeSelectManyTests.Shared_outer_inner_member_name_resolves_to_distinct_values`). **Mechanism, explicit-result-selector / query-syntax form:** the SAME bare-nav shape reaches `TranslateSelectMany` for both the explicit 2-arg overload and the query-syntax equivalent (they normalize identically). `TranslateSelectMany` first tries `NativeSelectManyBinder.TryBindBareNavUnwind` — it accepts a bare owned-collection nav selector (`o => o.Items.AsQueryable()`, no nested `Select`) purely on STRUCTURE, sets `UnwindSource`, and hands EF the `TransparentIdentifier(Outer, Inner)` shape this overload always expects (an item shaper built from a `StructuralTypeShaperExpression` over the owned nav's target entity type — never itself materialized when the trailing projection binds natively; it exists only so this method's return type-checks, and so an UNSUPPORTED trailing projection still folds through EF's own `ReplacingExpressionVisitor` mechanism for the driver-LINQ fallback). The REAL projection is bound separately: `TranslateSelect`'s pending-SelectMany-projection branch (see the post-terminal-invariant note above — it runs BEFORE the ordinary post-terminal guard, load-bearing for exactly this reason) checks `UnwindSource != null && Projection.Count == 0` and calls `NativeSelectManyBinder.TryBindTransparentIdentifierProjection`, which two-scope-binds each `ti.Outer.<m>`/`ti.Inner.<m>` leaf — re-rooting onto a synthetic per-scope parameter first, since `MongoExpressionTranslator.TryTranslateField` only accepts a `MemberExpression` whose own `Expression` is a bare `ParameterExpression` — using the SAME two structurally-separate translators the inner-`Select` form uses. **Both forms:** `Route` resolves to `NativeRoute.Projection` exactly like a plain projected `Select` (only the new `MongoUnwindFieldStage`, emitted immediately before the `$project`, is SelectMany-specific). `UnwindSource != null` joins the shared `HasTerminalOperator` gate (`IsGroupBy || IsDistinct || IsSetOp || Grouping != null || UnwindSource != null`) — a native SelectMany is terminal-only, exactly like `Distinct`/`GroupBy`/`Union`/`Concat`. **The mandatory unwrap-`Select`.** Because EF always wraps the result in `TransparentIdentifier(Outer, Inner)`, a `.Select(ti => ti.Inner)` (inner-`Select` form) or the trailing projected `Select` (explicit form, bound as above) unconditionally follows in the same translated tree; both forms build their `TransparentIdentifier` shaper up front so this reaches `TranslateSelect`/`MongoProjectionBindingExpressionVisitor` with no further bespoke unwrap-handling code needed beyond what's described above. **Whole-inner-entity projection hard-declines cleanly, and this covers all three equivalent user spellings:** `SelectMany(o => o.Items, (o, i) => i)`, `from o in q from i in o.Items select i`, and the bare 1-arg `SelectMany(o => o.Items)` all normalize to the identical tree — `UnwindSource` set, `Projection` still empty, trailing selector a bare `ti => ti.Inner` member access. This is neither an anonymous/DTO construction (`TryBindTransparentIdentifierProjection` rejects it) nor left to the ordinary post-terminal guard (`IsTransparentIdentifierMemberAccessSelector(selector)` is true for exactly this shape, so that else-if is skipped too) — a dedicated check ("`UnwindSource` set, `Projection` still empty, AND the selector is this whole-inner-entity shape") throws a plain `NotSupportedException` directly, at TRANSLATION time, before `MongoQueryMode` is even consulted, so the SAME exception fires in `Native`, `DriverLinq`, and `NativeOnly` alike, regardless of tracking. This is a **behavior improvement, not a new limitation**: a prior pass called `MarkNotNativelyRepresentable()` here (the ordinary graceful fallback), but empirically neither the native path (a bare owned entity has no whole-unwind shaper) nor the "graceful" driver-LINQ fallback (its shaper has no owning-entity `bsonDoc` context either) can materialize this shape — both crashed with an internal `KeyNotFoundException("The given key 'bsonDoc' was not present in the dictionary")` instead of declining cleanly; the clean `NotSupportedException` replaces that crash in every mode, the shape remaining exactly as unsupported as before. **Composition after the SelectMany is a separate, narrower story, and — empirically (EF-347 Task 3) — DIFFERS BETWEEN THE TWO FORMS** (see `NativeSelectManyTests`' `*_after_SelectMany_*`/`Explicit_result_selector_form_followed_by_*` tests, one per operator per form): for the **inner-`Select` form**, `Where`/`OrderBy`/`Skip`/`Take`/`GroupBy`/another `SelectMany` composed after it ALL still hard-fail in every mode — their own guards correctly reach `HasTerminalOperator` and call `MarkNotNativelyRepresentable()`, but the driver-LINQ fallback that guard would normally permit cannot rebuild a shaper that re-reads this form's already-by-index-resolved projection (its captured method chain carries an EF-synthesized `.Select(ti => ti.Inner)` unwrap over an internal `TransparentIdentifier` CLR type the driver's own LINQ bridge cannot map) — except a **cardinality-only** operator like `Count`, which needs no shaper at all, so its fallback succeeds for real and returns the correct result under `Native`/`DriverLinq`, throwing only under `NativeOnly`. For the **explicit-result-selector / query-syntax form**, `Where`/`OrderBy`/`Skip`/`Take`/`Count` composed after it instead fall back GRACEFULLY (correct results under `Native`/`DriverLinq`, throwing only under `NativeOnly`) — the difference is structural: nav-expansion produces exactly `SelectMany(bareNav, trivialResultSelector).Select(ti => new{...}).<op>(...)`, a chain the driver's own LINQ v3 provider already knows how to translate on its own, with no extra `ti.Inner`-unwrap hop over an internal type to trip over. `GroupBy` and a further `SelectMany` composed after the explicit form, however, still hard-fail in every mode, same as the inner-`Select` form — `GroupBy` for the same shaper-rebuild reason (its own `TranslateGroupBy` override bypasses the ordinary catch-all guards, and its fallback still cannot re-read the prior projection), and a further `SelectMany` because its OWN collection-selector shape is independently rejected/untranslatable regardless of what precedes it. A computed/non-member projection leaf also differs by form: the inner-`Select` form rejects it structurally at `TryBind` (hard-fails in every mode — `Computed_leaf_hard_fails_in_every_mode`), while the explicit form's bare-nav bind succeeds on structure alone before the trailing projection is even examined, so only the SEPARATE trailing-`Select` bind rejects the computed leaf and calls the ordinary `MarkNotNativelyRepresentable()` — falling back gracefully with correct results under `Native`/`DriverLinq` (`Explicit_result_selector_form_computed_leaf_falls_back_gracefully_except_under_NativeOnly`). **Deferred (out of scope, both forms):** a **correlated** collection selector (referencing anything beyond the owner/element pair), and a **nested** SelectMany (an owned collection inside another owned collection) — both keep hard-failing exactly as before, not a gap newly introduced by either slice. The explicit-result-selector / query-syntax form and the bare 1-arg `SelectMany(o => o.Items)` are **no longer deferred** as of slice 4 — see the mechanism and whole-inner-entity notes above for how each now resolves. A `SelectMany` over a **cross-collection reference** (non-owned) navigation is covered separately by **EF-347 slice 5** — the projected, FK-correlated form now goes native (see the note immediately below); a bare-entity reference result, a filtered/correlated-beyond-FK inner, and a nested reference SelectMany stay deferred.

> **Bare whole owned-collection-element `SelectMany` (EF-347, `selectmany-bare-owned` slice).** Native now ALSO covers the whole-inner-entity shape the note immediately above declines with a `NotSupportedException` — but only for an OWNED (embedded) collection, and only when the element itself carries no further navigations. All three equivalent user spellings — `from o in q from i in o.Items select i`, `SelectMany(o => o.Items, (o, i) => i)`, and the bare 1-arg `SelectMany(o => o.Items)` — normalize to the identical tree (`UnwindSource` set, `Projection` empty, trailing selector a bare `ti => ti.Inner`/`Include`-wrapped `ti.Inner` member access recognized by `TryGetWholeEntityMemberAccess`, which unwraps through any `IncludeExpression` layers first). **Gate:** in `TranslateSelect` (`Visitors/MongoQueryableMethodTranslatingExpressionVisitor.cs`), when `wholeEntityMember is { Member.Name: "Inner" }` AND `wholeElementCandidateUnwind.Kind == MongoUnwindSourceKind.Owned` AND `IsWholeElementRepresentable(innerEntityType, kind)` all hold, the code sets `UnwindSource.WholeElement = true` and falls through to the ordinary shaper fold, instead of throwing — the whole-OUTER (`select o`/`(o,i)=>o`) shape still falls to the `else if` and throws the SAME `NotSupportedException` as before (see `IsWholeElementRepresentable`'s XML doc and `Whole_outer_entity_form_still_declines_cleanly_in_every_mode` above). **(EF-347 Task 4 extends this SAME gate + `IsWholeElementRepresentable` to admit a REFERENCE-kind whole-inner result too — superseding the "REFERENCE-kind whole-inner result... still declines" wording that used to appear at this point; see the dedicated Task 4 note below.)** **Mechanism:** the lowerer emits `$unwind(includeArrayIndex: true)` then `$replaceRoot($mergeObjects)` (`MongoReplaceRootStage`) so the unwound owned element becomes the ROOT document, with the owner's key and the array ordinal merged in under sentinel fields `MongoReplaceRootStage.OwnerKeyField` (`"__ownerKey"`) / `.OrdinalField` (`"__ord"`) — carried along so the owned key can still materialize non-null (EF Core's own no-tracking null-owned-key guard would otherwise reject a partially-null owned key). `MongoShapedQueryCompilingExpressionVisitor.VisitShapedQuery` (`Visitors/MongoShapedQueryCompilingExpressionVisitor.cs`) checks `mongoQueryExpression.Select.UnwindSource is { WholeElement: true }` **before** its ordinary `projectedEntityType == null` fallback branch (load-bearing for a SHARED-TYPE owned element: `FindEntityType(clrType)` returns `null` for a CLR type reused by multiple owners, but `UnwindSource.InnerEntityType` — the binder's own `navigation.TargetEntityType` — is correct regardless) and roots the STANDARD `MongoProjectionBindingRemovingExpressionVisitor` at the owned element's `IEntityType` (not the owner's), forcing the DOM shaper (`allowStreaming: false` — collection elements are streaming-ineligible regardless, and `StreamingEligibility` would otherwise wrongly evaluate against the OUTER root). No new shaper type: this is the SAME projection-binding-removing visitor every whole-entity root query uses, just constructed with a different root entity type. `CreateGetValueExpression` (`Visitors/MongoProjectionBindingRemovingExpressionVisitor.cs`) reads the owned key back from the sentinel fields directly — gated on `entityType == _rootEntityType`, which is true only in this re-rooted case (the query root, `CollectionExpression.EntityType`, is always a document-root type, never an owned type) — reading the ordinal sentinel as `long` (`includeArrayIndex`'s BSON type) and converting, and the owner-key sentinel at the property's own CLR type. **Mandatory `AsNoTracking()`, and why:** a TRACKING query for this shape still throws — now `InvalidOperationException`, EF Core's OWN "owned entities cannot be tracked without their owner" guard (fires at shaper-materializer-injection time, BEFORE the native-vs-fallback split has any bearing), not a provider-authored guard — see `Bare_SelectMany_tracking_query_throws_InvalidOperationException_in_every_mode`. The provider does nothing to force `AsNoTracking()`; it is EF Core's existing contract for owned entities projected without their owner, applying here exactly as it would to any other bare-owned-entity projection. **No driver-LINQ oracle:** the same shape crashes the "graceful" fallback with the identical internal `KeyNotFoundException("...'bsonDoc'...")` the pre-existing decline note above documents, so this shape (like reference SelectMany and Intersect/Except) is proven via `MongoQueryMode.NativeOnly` succeeding plus an expected-in-memory-result-set assertion, not `Native == DriverLinq` parity — see `Bare_owned_whole_inner_element_goes_native_all_three_spellings`/`..._owner_with_zero_items_contributes_no_rows`/`..._reads_root_relative_not_owner_scoped`. **Shared-type owned collections** (one owned CLR type reused by multiple owners/navigations) also go native via this mechanism — see `Bare_owned_whole_inner_element_over_shared_clr_type_goes_native`. **`IsWholeElementRepresentable`** (`Query/Visitors/MongoQueryableMethodTranslatingExpressionVisitor.cs`) narrows acceptance with two empirically-found guards, each a clean decline rather than silent corruption or a confusing crash: (1) `!innerEntityType.GetNavigations().Any()` — a NESTED owned reference/collection under the element does not materialize correctly (the re-rooted element shaper still binds through the query's ROOT `ProjectionMember`, which resolves to the OUTER/owner entity's own `EntityProjectionExpression`, not the re-rooted element's; a nested navigation reaches EF's auto-`IncludeExpression` machinery and throws `InvalidOperationException` — see `Bare_owned_whole_inner_element_with_nested_owned_reference_member_declines_cleanly`; **this is a genuinely deferred follow-up, NOT delivered by this slice**, tracked as future work, not a regression); (2) no property whose configured element name collides with either sentinel field (`p.GetElementName() is not (OwnerKeyField or OrdinalField)`) — the `$mergeObjects` merges the sentinel object AFTER the unwound element, so a same-named real stored field would be SILENTLY OVERWRITTEN (unlike the Intersect/Except `_a`/`_b` source tags, which live as siblings of a wrapping `_doc` field and never collide with real element names) — see `Bare_owned_whole_inner_element_with_sentinel_collision_declines_cleanly`. Both guards fail closed into the SAME `NotSupportedException` the whole-outer/reference decline throws, at translation time, in every `MongoQueryMode`. **Multi-version:** no `#if` — identical EF8/EF9/EF10 behavior (all touched types `internal`). **Still deferred/declined, precisely:** whole-OUTER `select o` / `(o,i)=>o` (declines with `NotSupportedException`, unchanged from the note above); an owned element with a NESTED owned navigation (declines cleanly via `IsWholeElementRepresentable`'s navigation guard — a deferred follow-up, not this slice); an owned element with a real property whose element name collides with `__ownerKey`/`__ord` (declines cleanly via the same guard's sentinel-collision check); a COMPUTED projection leaf, a FILTERED/correlated-beyond-FK inner, and a NESTED `SelectMany` (all unchanged, still out of scope per the notes above); and any operator composed AFTER this bare-entity SelectMany (still hard-fails via the existing SelectMany-after-terminal `HasTerminalOperator` guard, same as every other terminal SelectMany shape). **A REFERENCE (cross-collection) bare-entity result is NO LONGER in this deferred list — see the EF-347 Task 4 note immediately below, which supersedes it.**

> **Bare whole REFERENCE-collection-element `SelectMany` (EF-347 Task 4).** Native now ALSO covers the whole-inner-entity shape for a REFERENCE (cross-collection) unwind — `db.Owners.SelectMany(o => o.Refs)` / `SelectMany(o => o.Refs, (o, r) => r)` / `from o in q from r in o.Refs select r`, all three normalizing to the identical tree the owned-element note above describes. **Gate:** the SAME `TranslateSelect` check now admits `wholeElementCandidateUnwind.Kind is MongoUnwindSourceKind.Owned or MongoUnwindSourceKind.Reference`, and calls the kind-aware `IsWholeElementRepresentable(innerEntityType, kind)`. **`IsWholeElementRepresentable` for `Reference`:** a single, narrower guard than the owned case — `!innerEntityType.GetNavigations().Any(n => n.IsEagerLoaded)`. A plain LAZY inverse back-reference (e.g. `RefItem.Owner`, or Northwind's `Order.Customer`) is never auto-included by EF Core and materializes fine as null, so it does NOT block this shape; only a navigation EF would try to auto-include (reaching EF's `IncludeExpression` machinery, which binds against the re-rooted shaper's wrong `ProjectionMember` — the same failure mode the owned nav guard documents) is rejected. **Guard reachability — verified empirically in a later task, and worth recording precisely so this guard is never mistaken for dead code:** the eager-nav guard above is reachable, and fires, ONLY for an eager-loaded OWNED (embedded) sub-navigation on the reference element — that is structurally a single-hop `IncludeExpression(ti.Inner, ownedNav)`, which `TryGetWholeEntityMemberAccess`'s unwrap-through-`Include` loop reduces to the bare `ti.Inner` access, so it reaches this gate and the guard throws the dedicated translation-time `NotSupportedException` in every mode (see `NativeSelectManyTests.Reference_form_bare_entity_with_owned_embedded_navigation_declines_cleanly_via_representability_guard`). A genuine CROSS-COLLECTION `[AutoInclude]` navigation on the reference element does NOT reach this guard at all: materializing a real cross-collection eager `Include` requires EF's nav-expansion to inject an ADDITIONAL join step, producing a DOUBLY-NESTED `TransparentIdentifier` (`ti.Outer.Inner`) that `TryGetWholeEntityMemberAccess` does not recognize (`wholeEntityMember` comes back `null`) — so it instead declines via the ORDINARY `MarkNotNativelyRepresentable()` fallback path, which (absent any driver-LINQ oracle for cross-collection SelectMany) still throws — `InvalidOperationException` under `Native`/`DriverLinq`, a separate native-translation exception under `NativeOnly` — see `Reference_form_bare_entity_with_cross_collection_autoinclude_declines_cleanly_in_every_mode`. Both shapes decline safely, never silently wrong data; they just take different code paths — the guard is confirmed LIVE by the owned-embedded-sub-nav test specifically, even though only that one shape (not the cross-collection one) actually exercises it. The owned-only sentinel-collision / complex-property / owned-key-serialization guards do not apply to `Reference` — a reference element merges no `$mergeObjects` sentinels and has no owned-type shadow keys. **Mechanism:** the `$lookup` + inner-join `$unwind` were already appended by `AppendLookupStages` (EF-347 slice 5); the lowerer's `UnwindSource`/`WholeElement` block (`MongoSelectLowerer.Lower`) then appends a PLAIN `MongoReplaceRootStage` (`mergeOwnerKeySentinels: false` for `Reference` — the looked-up element is already a whole, independently-keyed document, unlike an owned element which needs its owner key + ordinal carried via sentinel fields) that promotes the unwound lookup element to the root document — this plain-`$replaceRoot` lowering was already built by Tasks 2-3; Task 4 is the QMTEV recognition + guard wiring that actually reaches it. `MongoShapedQueryCompilingExpressionVisitor`'s existing `UnwindSource is { WholeElement: true }` branch (kind-agnostic) roots the standard DOM shaper at `UnwindSource.InnerEntityType` — no shaper change needed. **Tracking works with no `AsNoTracking()` required** (unlike the owned case) — a reference element is an ordinary top-level entity with its own real stored key, not an owned type, so EF Core's owned-entity tracking restriction does not apply. **No driver-LINQ oracle** (same as the rest of reference `SelectMany` per the slice-5 note above), so this shape is proven via `MongoQueryMode.NativeOnly` succeeding plus an expected-in-memory-result-set assertion — see `NativeSelectManyTests.Reference_form_bare_entity_result_goes_native_all_three_spellings`/`..._owner_with_zero_children_contributes_no_rows`/`..._reads_root_relative_not_owner_scoped`/`..._emits_lookup_unwind_plain_replaceRoot`. **Tracking, verified directly:** `Reference_form_bare_entity_tracking_query_returns_tracked_entities` confirms the no-`AsNoTracking()`-required claim above end-to-end — a default-tracking query yields tracked instances, and a mutate-then-`SaveChanges` round-trip persists. **Retained declines, verified directly:** the whole-OUTER shape (`select o`/`(o,i)=>o`) and any operator composed after this bare-entity reference SelectMany still decline cleanly in every mode, unchanged — see `Reference_form_whole_outer_result_still_declines_cleanly_in_every_mode` and `Reference_form_bare_entity_followed_by_Where_hard_fails_in_every_mode`. This also flips a pre-existing Northwind spec test: `NorthwindSelectQueryMongoTest.SelectMany_without_result_selector_naked_collection_navigation` (`Customers.SelectMany(c => c.Orders)`) previously asserted a translation failure and now asserts the query succeeds natively with correct results, since `Order`'s own navigations are not eager-loaded in that fixture. **Multi-version:** no `#if` — identical EF8/EF9/EF10 behavior (all touched types `internal`). **Still deferred, unchanged:** a filtered/correlated-beyond-FK inner, a computed projection leaf, a nested reference `SelectMany`, and any operator composed after this bare-entity reference SelectMany (all per the slice-5 note above).

> **Cross-collection reference `SelectMany` — projected (EF-347 slice 5).** Native now also covers a **projected** `SelectMany` over a **cross-collection reference** (non-owned) collection navigation — `q.SelectMany(c => c.Orders, (c, o) => new {c.Name, o.Total})` and the query-syntax equivalent `from c in q from o in c.Orders select new {...}` — emitting `$lookup` + an **inner-join** `$unwind` (`preserveNullAndEmptyArrays: false`) + `$project`. **Recognition:** unlike an owned collection (read off the document as an embedded array), a reference collection selector arrives from EF's nav-expansion as a **correlated subquery** — `Queryable.Where(EntityQueryRootExpression<Target>, o => c.<pk> == o.<fk>)`, the target collection queried from its own root and filtered by the FK — NOT a bare nav member access. `NativeSelectManyBinder.TryBindReferenceNavUnwind` recognizes that shape via the shared `NativeCorrelationMatcher.TryMatchCorrelatedCollection(..., requireEmbedded: false)` (the SAME correlated-`Where` recognition the projected-`Count` binder uses — extracted to that shared helper in slice-5 Task 1, behavior-preserving for the `Count` binder), resolves the single matching reference navigation, registers a `ForceUnwind` `LookupExpression`, and sets a `Reference`-kind `MongoUnwindSource` whose `InnerScopePath` is the lookup's `_lookup_<Nav>` alias (the `Owned` kind keeps the embedded element path — the field was renamed `ElementPath`→`InnerScopePath` in slice-5 Task 2 to cover both). The mutation of `mongoQueryExpression` (register lookup + set `UnwindSource`) happens ONLY after the match is confirmed — a rejected shape leaves the query untouched. **Lowering:** `MongoSelectLowerer.AppendLookupStages` gained an `IsCollection && ForceUnwind` arm — provably disjoint from the two Include arms (`IsNativeCollectionLookup` requires `!ForceUnwind`; `IsStreamableReference` requires a reference/non-collection nav), so it catches exactly the reference-SelectMany lookup and reroutes nothing — that emits `MongoLookupStage` + `MongoUnwindStage(preserveNullAndEmptyArrays: false)`. `MongoUnwindStage` gained a `PreserveNullAndEmptyArrays` flag **defaulting to `true`** so the reference-**Include** `$unwind` (a LEFT join — a principal with no child still yields a row) is unchanged, while the SelectMany flatten passes `false` (an **inner** join — a principal with no children contributes NO rows, matching `SelectMany` semantics; see `NativeSelectManyTests.Reference_form_principal_with_zero_children_contributes_no_rows`). `Lower`'s `UnwindSource` block now emits the owned `MongoUnwindFieldStage` for `Owned` only; the `Reference` `$lookup`+`$unwind` are appended earlier by `AppendLookupStages`. Note the explicit LINQ `Join`/`GroupJoin`/`LeftJoin` path ALSO builds `IsCollection && ForceUnwind` lookups, but a join query never reaches the lowerer — it routes to `Fallback` via the `NativeSlotPopulator` catch-all (joins are not in `IsNativeRepresentableSlotOperator`) — so this arm is reached only by reference SelectMany. **Projection/shaper reuse:** the trailing `.Select(ti => new {...})` binds through the UNCHANGED slice-4 two-scope binder (`TryBindTransparentIdentifierProjection`) — `ti.Inner.<m>` resolves against the inner scope with the `_lookup_<Nav>` prefix (`$_lookup_Orders.Total`), `ti.Outer.<m>` against the root (`$Name`) — because the binder's `InnerScopePath` read is kind-agnostic; and the QMTEV's reference branch reuses the shared `BuildBareNavWrappedShaper` helper (also used by the owned bare-nav branch — a behavior-preserving extraction of that branch's body) to hand EF the `TransparentIdentifier(Outer, Inner)` shape it expects. `Route` stays `NativeRoute.Projection`; `UnwindSource != null` already joins the shared `HasTerminalOperator` gate, so a native reference SelectMany is terminal-only like the owned form. **Additive, and there is NO driver-LINQ baseline:** reference `SelectMany` hard-failed in every mode before this slice, AND the driver's own LINQ v3 provider rejects a cross-collection `SelectMany` outright (see `NativeSelectManyTests.Reference_form_has_no_driver_linq_fallback_and_still_throws_under_explicit_DriverLinq_mode`) — so, unlike the owned form (which the driver CAN translate), every unsupported reference shape and every operator composed AFTER the reference SelectMany **throws in every mode** (`Native`/`DriverLinq`/`NativeOnly`) rather than gracefully falling back: `Where`/`OrderBy`/`Skip`/`Take`/`Count`/`Distinct`/`GroupBy`/a second `SelectMany` after the reference SelectMany ALL hard-fail in every mode (see the `Reference_form_followed_by_*_hard_fails_in_every_mode` tests, including `Reference_form_followed_by_Distinct_hard_fails_in_every_mode` — contrast the owned explicit form, where `Where`/`OrderBy`/`Skip`/`Take`/`Count`/`Distinct` fall back gracefully because the driver can translate that chain). Per the versioning rubric the exception type of an unsupported shape is not part of the contract. A parameter captured in the OUTER query's predicate substitutes correctly through the reference pipeline (`Reference_form_parametrized_outer_predicate_composes_correctly_with_native_SelectMany`). **Multi-version:** EF8/EF9/EF10 behave identically (no `#if`). **Still deferred/hard-fail:** a **filtered or correlated-beyond-FK** inner (`c.Orders.Where(userPred)` — the matcher's exactly-one-correlation guard rejects the extra conjunct, so the user filter is never silently dropped), a **computed** projection leaf, and a **nested** reference SelectMany. **A bare-entity reference result (`(c,o) => o` / `select o`) is NO LONGER in this deferred list — EF-347 Task 4 made it go native (for a representable element — see the dedicated Task 4 note below); only the whole-OUTER shape (`(o,i)=>o`/`select o`) remains a clean decline.**

> **Non-canonical Skip/Take (EF-347).** Native now also covers filter/sort/paging compositions OUTSIDE the original fixed `$match → $sort → $skip → $limit` arrangement: a `Where`/`OrderBy`/`ThenBy` (or an aggregate-injected predicate) applied AFTER a `Skip`/`Take` already on the select, `Take` before `Skip`, and repeated/interleaved paging (`Skip(a).Take(b).Skip(c)`) — shapes that previously forced a driver-LINQ fallback purely because they didn't fit the one fixed slot arrangement. **Mechanism (Task 1):** `MongoSelectDefinition` replaced its single `Predicate`/`Orderings`/`Offset`/`Limit` slots with an ordered `PipelineOps` list (`MongoMatchOp`/`MongoSortOp`/`MongoSkipOp`/`MongoLimitOp`, in `Expressions/MongoSelectOp.cs`); **arrival order IS emission order** — `MongoSelectLowerer.AppendSelectOpStages` (renamed from `AppendCanonicalStages`) walks `PipelineOps` and emits each op verbatim, with no reordering or fixed-slot logic. **Merge rules**, all on `MongoSelectDefinition`: `AddPredicateConjunct` ANDs a `Where`/injected-predicate into the tail op if it is already a `MongoMatchOp` (so consecutive `Where`s still collapse to one `$match`, unchanged from before), else appends a new one; `StartOrReplaceSort` (`OrderBy`) REPLACES the tail op if it is a `MongoSortOp` (reproducing the old reset-orderings semantics for `OrderBy(a).OrderBy(b)`), else appends a fresh sort; `AppendThenBy` extends the tail sort; `AppendSkip`/`AppendLimit` (`Skip`/`Take`) always append. **Task 2** deleted the canonical-order rejection guards in `NativeSlotPopulator` (`PagingAlreadyApplied` on `Where`/`OrderBy`/`OrderByDescending`/`ThenBy`/`ThenByDescending`, and the single-slot `Skip`/`Take` checks) — each of the seven slot operators now just applies its merge rule unconditionally. **Task 3** deleted the symmetric guard in `NativeCardinalityBinder.TryBindAggregate` that forced fallback for a predicate-injecting aggregate (`All`, or predicate-bearing `Count`/`Any`) following `Take`/`Skip`: `AddPredicateConjunct` always ANDs into (or appends after) the CURRENT tail of `PipelineOps`, so the injected predicate lands after any already-recorded `$skip`/`$limit` — never hoisted ahead of it — meaning `Take(n).All(pred)`/`Count(pred)`/`Any(pred)` correctly evaluate only over the first `n` rows without a dedicated guard; a plain aggregate that injects no predicate (`Take(n).Count()`) was never affected either way. **This is coverage, not correctness:** every shape above already returned correct results via the driver-LINQ fallback (the oracle) — the change only widens which shapes execute via the native pipeline instead of falling back; no result changes. **The reorder is not a break:** faithful arrival-order emission means the MQL emitted for a shape that was ALREADY native before EF-347 can change stage order/shape versus a prior build (e.g. a merge landing differently once ops are recorded verbatim) — per the versioning rubric (see the top of this file) the exact emitted MQL for a supported, already-native query is an implementation detail, not part of the contract, so this is **not** a breaking change. **Retained boundaries, verified empirically (not just by inspection) via `MongoQueryMode.NativeOnly` probes:** `NativeGroupByBinder.TryBindGroupKey`'s pre-existing `select.HasPaging || select.HasOrdering` guard (line ~54; Task 1 only renamed `Orderings.Count > 0` → `HasOrdering`, the guard itself is untouched) still rejects **any** `GroupBy` — key or key+aggregate — reached after paging/ordering has already been recorded, canonical or non-canonical alike, so `Take(n).Skip(m).GroupBy(...)` still falls back gracefully exactly as before this slice. A plain projected `Select`, by contrast, was **never guarded on paging state** in either `TranslateSelect` or `NativeProjectionBinder` — so once Task 2 stopped forcing `Fallback` for the non-canonical paging itself, a projected `Select` composed after it (e.g. `OrderBy(...).Take(3).Skip(1).Select(new {...})`) goes native too, as an unplanned but harmless side effect of the paging relaxation, not a change to `NativeProjectionBinder` itself — `Select` simply never cared what paging shape preceded it. The `HasTerminalOperator` post-terminal gate (`IsGroupBy`/`IsDistinct`/`IsSetOp`/`Grouping`/`UnwindSource` — see the GroupBy as-built note above) is entirely orthogonal to this slice and unchanged: non-canonical Skip/Take is a *pre-terminal* relaxation only, and every post-terminal guard elsewhere in this document still applies exactly as documented.

In: top-level method-call translators **and native slot population**, the **query expression tree → stage IR → `BsonDocument[]` pipeline generator** (lowerer / renderer / pipeline factory), the compile-time **gate**, projection binding, the EF-to-driver-LINQ bridge (fallback), shaper compilation (native streaming + DOM, and driver-LINQ DOM), vector-search pre-extraction.
Out: BSON encoding/decoding of *values* (Serializers area — the renderer asks `BsonSerializerFactory` for serializers), change-tracking snapshots (ChangeTracking area), index/collection schema (Storage area), cursor execution itself (`MongoClientWrapper.Execute` in Storage), and aggregation-pipeline generation **for the fallback path** (still the driver's job).

## Pipeline at a glance

```
IQueryable<T>  (EF Core)
   │
   ▼  MongoQueryCompilationContext           (preserves the original LINQ tree; carries the MongoQueryMode)
   ▼  MongoQueryTranslationPreprocessor      (hoist final predicates; lift VectorSearch out before nav expansion)
   ▼  MongoQueryableMethodTranslatingExpressionVisitor (QMTEV)
   │      ├─ accepts only Queryable / MongoQueryableExtensions / MongoDB.Driver.Linq.MongoQueryable
   │      ├─ DELEGATES native slot population to NativeSlotPopulator.PopulateNativeSlots (the ordered
   │      │     PipelineOps list — match/sort/skip/limit ops recorded in arrival order, EF-347) and
   │      │     projection binding to NativeProjectionBinder.TryPopulateNativeProjection (Projection),
   │      │     both via MongoExpressionTranslator; either calls mongoQ.Select.MarkNotNativelyRepresentable()
   │      │     for any operator it does not lower into an op (catch-all) — see pitfalls
   │      └─ ALWAYS also captures the raw method chain (CapturedExpression) for the driver-LINQ fallback
   ▼  MongoQueryTranslationPostprocessor     (apply final ProjectionMapping)
   ▼  MongoShapedQueryCompilingExpressionVisitor  ── THE GATE (compile time, honors MongoQueryMode) ──
   │   if Native & Select.Route != Fallback & lower/render succeed → NATIVE PATH:
   │      MongoSelectLowerer  : PipelineOps → MongoPipelineStage[]  (emitted verbatim in arrival order, EF-347; then $lookup/$unwind/terminal)
   │      MongoQueryLanguageRenderer + PlaceholderTable : predicate → $match BSON; params → sentinels
   │      MongoPipelineFactory.Create : render ONCE into a template + placeholder table  (B2)
   │      compile streaming shaper (MongoStreamingEntityMaterializerRewriter) if StreamingEligibility, else DOM
   │   else (DriverLinq, or not representable, or NativeOnly→throws) → DRIVER-LINQ FALLBACK:
   │      MongoEFToLinqTranslatingExpressionVisitor rewrites CapturedExpression as driver-LINQ; DOM shaper
   ▼  MongoExecutableQuery + QueryingEnumerable
          ├─ NATIVE: factory.Build(parameterValues) → BsonDocument[] set as NativePipeline;
          │          MongoClientWrapper.Execute runs collection.Aggregate(pipeline) (RawBsonDocument if streaming, else BsonDocument)
          └─ FALLBACK: MongoClientWrapper.Execute hands the driver-LINQ expression to IMongoQueryProvider
          → IAsyncCursor; shaper turns each row into the requested CLR shape
```

B2 = the pipeline template is built **once at compile time** (lower + render → `MongoPipelineFactory`); only `factory.Build(parameterValues)` (clone-and-substitute the placeholder sentinels) runs per execution. The native-vs-driver and streaming-vs-DOM decisions are therefore **compile-time-deterministic** — there is exactly **one** shaper per query and **no** runtime dual-shaper dispatch.

## Key entry points

- `MongoQueryCompilationContext` — EF Core hook to preserve the original captured `IQueryable` so post-translation we can recover the user's intent (e.g. for diagnostics).
- `MongoQueryTranslationPreprocessor` — first phase. Notably, **`VectorSearch(...)` is extracted before EF's nav-expansion and re-inserted after** — nav expansion doesn't know about it.
- `Visitors/MongoQueryableMethodTranslatingExpressionVisitor` — the central LINQ-method dispatcher. Enforces the allowed-method-source set; **delegates native slot population and projection binding** to `NativeSlotPopulator`/`NativeProjectionBinder` (the `Translate*` overrides that would otherwise do this are inert — see pitfalls); captures the final method-chain expression on `MongoQueryExpression`.
- `Visitors/MongoShapedQueryCompilingExpressionVisitor` — **the compile-time gate**. Reads `MongoQueryMode`; consults `MongoSelectDefinition.Route` (the authoritative is-native decision for the slot/projection representability) to decide native vs. fallback; builds the `MongoPipelineFactory` and compiles the native (streaming/DOM) shaper when `Route != NativeRoute.Fallback`, else compiles the driver-LINQ fallback shaper. Splits scalar vs. entity result paths. **The full is-native decision is centralized (EF-334) in `ClassifyNativeDisposition`** → `NativeDisposition {Native, Fallback, HardDecline}`, which reads the three is-native signals in one place: `Route` (slot/projection representability), `IsGroupByFallbackUnsafe` (the GroupBy+Join wrong-data hard-decline), and vector search (`ContainsVectorSearch(CapturedExpression)` — the one signal not on `MongoSelectDefinition`, since the `VectorSearch(...)` call is lifted out by the preprocessor and never reaches slot population). `Route` stays "which native shape / slot representability"; the disposition is the superset. Cross-collection `$lookup` streamability (`AllPendingLookupsAreStreamable`) is **not** an is-native signal — it is the separate streaming-vs-DOM axis (it only picks the streaming shaper for a query that is already native), so it is deliberately outside the disposition.
- **Native translation (`NativeTranslation/` + `Expressions/`):**
  - `Expressions/MongoExpression` + subtypes (`MongoFieldExpression`/`MongoConstantExpression`/`MongoParameterExpression`/`MongoBinaryExpression`/`MongoUnaryExpression`) + `MongoOrdering` — the dialect-agnostic `SqlExpression`-analog node hierarchy.
  - `Expressions/MongoQueryExpression` (+ `.Lookup.cs`) — the structured query tree: it **has-a** `MongoSelectDefinition` (reached via `MongoQueryExpression.Select`) holding the native scalar IR, **plus** the cross-collection `$lookup`/inner-collection state (`Lookups`/`IsJoinQuery`/`InnerCollections`, on `.Lookup.cs`) and the retained `CapturedExpression` + `ProjectionMapping` for the fallback.
  - `Expressions/MongoSelectDefinition` — the native-translation logical query IR — an ordered `PipelineOps` list (`MongoMatchOp`/`MongoSortOp`/`MongoSkipOp`/`MongoLimitOp`, replacing the original single `Predicate`/`Orderings`/`Offset`/`Limit` slots as of EF-347 — see the Non-canonical Skip/Take note above); once a set op is attached, `MongoSelectDefinition` also holds a SECOND ordered op list, **`TrailingOps`** (EF-347 slice B — see the set-ops as-built notes below), populated by the SAME five merge methods (`AddPredicateConjunct`/`StartOrReplaceSort`/`AppendThenBy`/`AppendSkip`/`AppendLimit`) once they target the private **`ActiveOps`** property instead of `PipelineOps` directly — `ActiveOps` is `PipelineOps` normally and flips to `TrailingOps` the instant `SetOperation` is attached, so a `Where`/`OrderBy`/`ThenBy`/`Skip`/`Take` (or an aggregate-injected predicate) composed AFTER a set op records into `TrailingOps` and is emitted by the lowerer AFTER the set-op stage, over the COMBINED result; **`IsSetOpTerminalOnly`** (`IsSetOp && !IsGroupBy && !IsDistinct && Grouping == null && UnwindSource == null`) is the narrow predicate that relaxes the two post-terminal catch-all guard sites (`NativeSlotPopulator`'s slot guard, `NativeCardinalityBinder.TryBindReducer`/`TryBindAggregate`) to admit exactly those trailing operators while every other deferred own-`Translate`-override operator keeps its own untouched `HasTerminalOperator` check — plus **`Projection`** — extracted (EF-330) from `MongoQueryExpression` into a composed, dialect-neutral data-holder. `NativeSlotPopulator`/`NativeProjectionBinder` populate it via `mongoQ.Select`; the gate and lowerer read it. The `$lookup` state stays on `MongoQueryExpression` (entangled with the fallback shaper, slated for EF-317), so it was **not** moved here. **`Projection` (EF-331)** holds the dialect-neutral `MongoProjection` (alias → `MongoExpression` field-ref pairs) for a terminal anonymous-type/DTO member-access `Select`; it is populated by `NativeProjectionBinder.TryPopulateNativeProjection` (not by `NativeSlotPopulator.PopulateNativeSlots` — see the catch-all pitfall below) and stays empty for every other projection shape (bare scalar, computed, mixed, non-terminal), which still fall back to driver-LINQ. The single authoritative is-native decision is the computed property `Route` (enum `NativeRoute`: `Fallback`/`WholeEntity`/`Projection`/`ScalarAggregate`/`GroupBy`) — `Fallback` when an internal `_hasUnsupportedOperator` flag was set via `MarkNotNativelyRepresentable()`; otherwise `ScalarAggregate` (EF-SP4) when a scalar-aggregate `Cardinality` is set, `GroupBy` (EF-SP6) when a `Grouping` is set, `Projection` when `Projection` is non-empty (representable **and** has a pushed-down projection); otherwise `WholeEntity`. `Route` is the authoritative signal for **slot/projection** representability; the full is-native decision is the gate's `ClassifyNativeDisposition` (EF-334), which layers two further is-native signals onto `Route`: vector search (via `ContainsVectorSearch` over `CapturedExpression` — not on `MongoSelectDefinition`, as the `VectorSearch(...)` call is lifted out before slot population) and the GroupBy+Join hard-decline (`IsGroupByFallbackUnsafe`). `$lookup` streamability (`AllPendingLookupsAreStreamable`) is a **separate** axis (streaming-vs-DOM), not an is-native signal.
  - `NativeTranslation/MongoExpressionTranslator` — EF predicate / key-selector body → `MongoExpression` (`TryTranslate` / `TryTranslateField`); returns false for anything outside its acceptance set. As of EF-329 (predicate breadth) that set includes nullable equality/`== null` (IS NULL), `Enumerable.Contains`/`ICollection.Contains` → `MongoInExpression` (`$in`/`$nin`), `string.StartsWith`/`EndsWith`/`Contains` → `MongoRegexExpression` (`$regularExpression`), and field-to-field/arithmetic `MongoBinaryExpression`s (still parity-limited otherwise; the computed long tail — string transforms, date parts, `Math.*`, type-changing casts — remains unsupported and falls back).
  - `NativeTranslation/MongoSelectLowerer` — the native IR → `MongoPipelineStage[]` (typed IR under `NativeTranslation/Stages/`), BSON-free; emits the recorded `Select.PipelineOps` verbatim in arrival order (EF-347 — no fixed canonical order) + owns the lookup eligibility guards. When `MongoSelectDefinition.Projection` is populated, the lowerer appends a `NativeTranslation/Stages/MongoProjectStage` **last** in the pipeline (after `$match`/`$sort`/`$skip`/`$limit`), carrying the `MongoProjection` alias/field-ref pairs; `MongoPipelineFactory.Create`'s stage-walk renders it to a `$project` BSON body (explicit `_id: 0` plus one entry per alias) in the aggregation-expression field-ref dialect.
  - **`NativeTranslation/MongoQueryLanguageRenderer` + `NativeTranslation/MongoAggregationExpressionRenderer` — the dual-dialect renderer (EF-329 predicate breadth).** `MongoQueryLanguageRenderer` renders `MongoExpression` → `$match`-dialect BSON (`{field:{$gt:v}}`, `$and`/`$or`, bare-bool, `$ne`, `$in`/`$nin`, `$regularExpression`) — the query/filter dialect, and is usable directly as a `$match` stage body. `MongoAggregationExpressionRenderer` renders a `MongoExpression` subtree to the **aggregation-expression** dialect that lives inside `{ $expr: … }` (`$eq`/`$ne`/`$lt`/…, `$add`/`$subtract`/`$multiply`/`$divide`/`$mod`, field refs as `"$elementName"`) — the only dialect that can express field-to-field comparisons and arithmetic, which have no query-dialect form. `PlaceholderTable` (shared by both) records parameter placeholders. Both renderers share one value-rendering path, `NativeTranslation/MongoValueRenderer`, for `MongoConstantExpression`/`MongoParameterExpression` leaves, so a constant and a parameter of the same value always emit identical BSON regardless of which dialect is in play; `MongoAggregationExpressionRenderer` is a `static` class (no instance state, unlike `MongoQueryLanguageRenderer`).
    - **Per-subtree boundary is index-first, and the renderer — not the lowerer — owns it.** `MongoQueryLanguageRenderer.RenderNode` keeps `&&`/`||` (`AndAlso`/`OrElse`) at the query level always: each branch of an AND/OR is rendered (and combined via `CombineAnd`/`CombineOr`) independently, so a mixed predicate like `a.Age > 21 && a.X == a.Y` keeps the indexable `Age` clause as a plain `$match` term and wraps only the non-query-expressible `X == Y` subtree in `$expr` (`RenderAsExpr` delegates that one subtree to `MongoAggregationExpressionRenderer`). This maximizes index usage: `$expr` clauses can't use indexes as effectively as native `$match` operators, so it is scoped to the *smallest* subtree that actually needs it, not promoted to the whole predicate. A comparison is query-native (handled by `MongoQueryLanguageRenderer` directly) only when the left side is a bare field and the right side is a constant/parameter (`IsQueryNativeComparison`); anything else (field-to-field, arithmetic operands) falls through to `RenderAsExpr`.
    - **This is an intentional refinement over the original design doc.** `docs/superpowers/specs/2026-06-23-native-query-provider-design.md` (§"Lowering, rendering & the dialect boundary") states dialect choice is made in the lowerer, never in the QMTEV or the node types. With per-clause dialect *mixing* now required (a single predicate can straddle both dialects), that responsibility moved to the **renderer**, which walks the already-lowered, dialect-neutral `MongoExpression` tree and decides per-subtree which dialect applies. `MongoSelectLowerer` and the `MongoExpression` node types remain completely dialect-neutral, as originally designed — only the *renderer* is dialect-aware, and only it needed to move to support mixing.
  - `NativeTranslation/MongoPipelineFactory` — the B2 template: `Create(stages, renderer)` walks the stages into a `BsonDocument[]` template (+ placeholder table); `Build(parameterValues)` deep-clones and substitutes per execution. Validates `$limit>0`/`$skip≥0` (throws `ArgumentOutOfRangeException`, matching EF).
  - `NativeTranslation/MongoStreamingEntityMaterializerRewriter` + `BsonRowReader` + `StreamingEligibility` — forward-only `RawBsonDocument` → POCO materialization for eligible entities (no DOM build).
  - `Infrastructure/MongoQueryMode` (`Native`/`DriverLinq`/`NativeOnly`) — the user-facing gate option (`UseQueryMode`).
  - `NativeTranslation/NativeCardinalityBinder` (EF-SP4) — populates `MongoSelectDefinition.Cardinality` for entity reducers (`TryBindReducer`: synthesizes the reducer `$limit`) and scalar aggregates (`TryBindAggregate`: synthesizes the aggregate, and for `All`/predicate-bearing `Count`/`Any` injects the (negated) predicate as a `$match` conjunct via `AddPredicateConjunct`). As of EF-347 Task 3, a predicate-injecting aggregate following a `Take`/`Skip` already on the same select goes native too — `AddPredicateConjunct` always ANDs into (or appends after) the TAIL of the ordered `PipelineOps` list, i.e. after any already-recorded `$skip`/`$limit`, never hoisted ahead of it — see the Non-canonical Skip/Take as-built note above. The only guard `TryBindReducer`/`TryBindAggregate` still own is the pre-existing `HasTerminalOperator` check (a reducer/aggregate after `GroupBy`/`Distinct`/a set-op/`SelectMany` still falls back) — see the scalar-cardinality as-built note above.
  - `Expressions/MongoCardinality` (on `MongoSelectDefinition.Cardinality`) — the dialect-neutral IR for "this select terminates in a reducer or aggregate rather than a row sequence": `Reducer`/`Aggregate` kind, `Selector` field-ref, `EmptyBehavior`/`EmptyValue`, `ResultType`.
  - `NativeTranslation/Stages/MongoCountStage` / `MongoGroupAccumulatorStage` — typed IR stages the lowerer appends for `Count`/`LongCount`/`Any` (`$count`) and `Sum`/`Min`/`Max`/`Average` (`$group` with the matching accumulator) respectively, mirroring how `MongoProjectStage` is appended for projections.
  - `NativeTranslation/NativeGroupByBinder` (EF-SP6) — populates `MongoSelectDefinition.Grouping` for `GroupBy(key).Select(aggregate)`, mirroring `NativeCardinalityBinder`'s role for scalar aggregates. `TryBindGroupKey` parses the key (scalar `MemberExpression` / composite anonymous `NewExpression`) into `PendingGroupKey`; `TryBindGroupProjection` + `TryBindAccumulator` parse the aggregate `Select`, finalize the `MongoGrouping`, and emit the flattening projections. Holds the two correctness guards — `HasDefaultKeySerialization` (reject value-converter / non-default-`BsonRepresentation` keys) and `IsGroupingSource` (accumulator source must be the grouping parameter `g`).
  - `Expressions/MongoGrouping` (on `MongoSelectDefinition.Grouping`) — dialect-neutral `$group` IR: `Key` parts (`MongoGroupingKeyPart`; null `Name` ⇒ scalar `_id`, named parts ⇒ composite `_id` sub-doc) + `Accumulators` (`MongoGroupAccumulator`: output field, operator `$sum`/`$avg`/`$min`/`$max`, null operand ⇒ `$sum:1`). Lowered by the `MongoSelectLowerer` group branch to `NativeTranslation/Stages/MongoGroupStage`, rendered by `MongoPipelineFactory.RenderKeyedGroup` (composite → `_id` sub-document, else bare field-ref) in the aggregation-expression dialect.
  - `Expressions/MongoElementRefExpression` (EF-SP6) — a raw document-element reference by (possibly dotted) path with **no** backing `IProperty`, rendering as `"$" + Path` in the aggregation-expression dialect. The GroupBy flattening `$project` uses it to read each group output — `_id` (scalar key), `_id.<Name>` (composite sub-key), or an accumulator output field — up into a top-level result alias for the DOM shaper.
  - `EnumerableMethods` (provider root, `src/MongoDB.EntityFrameworkCore/EnumerableMethods.cs`) — the provider's own port of EF Core's `internal` `EnumerableMethods`; **extended for EF-SP6** with the `Count`/`LongCount`/`Sum`/`Average`/`Min`/`Max` selector members (`IsSumWithSelector` etc.) that `NativeGroupByBinder.TryBindAccumulator` matches grouped aggregates against (both the `Enumerable` and the EF-lowered `Queryable`-over-`AsQueryable()` forms).
  - `Visitors/MongoShapedQueryCompilingExpressionVisitor.ExecuteAggregate` / `TryBuildAggregateFactory` — compile-time construction of the scalar-result execution path: builds the pipeline factory, applies `MongoCardinality.EmptyBehavior`/`EmptyValue` around the single-row-or-absent aggregate result, and deserializes via `DeserializeScalar<TResult>` (non-int scalar coverage, e.g. `decimal`/nullable numerics).
  - `SingleValueEnumerable` — the `IEnumerable<T>` wrapper the native reducer/aggregate path returns so EF Core's own base cardinality reduction (First/Single throw/empty semantics) runs unmodified over a zero-or-one-element sequence.
- `Visitors/MongoEFToLinqTranslatingExpressionVisitor` — converts the residual EF expression into a tree the driver's LINQ v3 provider understands (`Mql.Field`, parameter resolution, `As<T>(serializer)`).
- `Visitors/MongoProjectionBindingRemovingExpressionVisitor` — replaces `ProjectionBindingExpression` nodes with concrete index-based reads from a `BsonDocument`.
- `Visitors/MongoMixedProjectionBindingRemovingExpressionVisitor` — sibling of the above used on the mixed path (projection contains entity references LINQ v3 can't handle); `MongoShapedQueryCompilingExpressionVisitor` strips the trailing `Select` and dispatches to this visitor so the shaper runs client-side over full `BsonDocument`s.
- `Expressions/MongoCollectionExpression`, `EntityProjectionExpression`, `RootReferenceExpression`, `ObjectAccessExpression`, `ObjectArrayProjectionExpression` — provider-specific expression nodes for collection roots, nested-document access, and entity shape.
- `MongoExecutableQuery` + `QueryingEnumerable` — the compiled-query handoff. `QueryingEnumerable` calls `MongoClientWrapper.Execute(MongoExecutableQuery)` and applies the shaper. For the native path `MongoExecutableQuery` carries `NativePipeline` (the built `BsonDocument[]`), `Streaming`, and `Session` (internal); for the fallback it carries the driver-LINQ `Query` expression.
- `Factories/*` — EF Core DI factories registered in `MongoServiceCollectionExtensions.AddEntityFrameworkMongoDB()`.

## Boundaries with adjacent areas

- **vs Storage.** Query stops at the executable query — it produces *data*: either a native `BsonDocument[]` pipeline (`MongoExecutableQuery.NativePipeline`) or a driver-LINQ expression. `MongoClientWrapper.Execute(...)` (Storage) owns execution: it runs `collection.Aggregate(nativePipeline)` for the native path and hands the expression to `IMongoQueryProvider` for the fallback. Cursor lifecycle, transaction/session binding, retries are Storage + driver. Never call `IMongoCollection<>.Aggregate(...)` directly from Query — build the `BsonDocument[]` and let the wrapper run it.
- **vs Storage — bulk `ExecuteUpdate`/`ExecuteDelete` (EF9+) uses a *second*, deliberately different seam.** Reads cross to Storage as **data** (a `MongoExecutableQuery`); bulk crosses as **behavior** — `MongoShapedQueryCompilingExpressionVisitor.CreateBulkPlan<TSource>` builds a `MongoBulkPlan` (Storage) carrying `Func<QueryContext, …>` translation delegates, and `MongoBulkOperationExecutor` (Storage) invokes them at runtime. This is intentional: bulk filter/update translation is parameter-value-dependent, so it must be deferred and re-run per execution. The translation helpers (`BuildFilter`/`BuildUpdate`/`BuildIdDocumentQuery`) stay here in Query; only their rendered results cross the boundary. Don't "fix" the bulk seam to look like the read seam — they differ for this reason.
- **vs Serializers.** Query needs an `IBsonSerializer` for every materialized projection result — it asks `BsonSerializerFactory` (Serializers area). Query should never instantiate serializers directly.
- **vs Metadata.** `IEntityType`, `IProperty`, navigation info come from Metadata. Query reads `GetElementName()`, `GetBsonRepresentation()`, `GetDiscriminator*()` etc. — it doesn't write them.
- **vs the driver's LINQ v3 provider.** Only the **fallback** path uses it: Query produces a driver-LINQ expression (types from `MongoDB.Driver.Linq`) and hands it off, and the driver builds that pipeline. The **native** path bypasses the driver's LINQ provider entirely — the provider builds the `BsonDocument[]` itself (`MongoSelectLowerer` + `MongoQueryLanguageRenderer` + `MongoPipelineFactory`) and only uses the driver as a BSON/cursor/protocol library via `Aggregate`. The deliberate, stable boundary is **our query tree → `BsonDocument[]` → `Aggregate`**; we do **not** build on the driver's internal AST.

## Common pitfalls

- **Allowed method sources are strict.** Only `Queryable`, `MongoQueryableExtensions`, and `MongoDB.Driver.Linq.MongoQueryable` methods are accepted. Anything else throws `NotTranslatedExpression` early.
- **VectorSearch extraction.** `VectorSearch(...)` must be pulled out of the tree before EF's nav-expansion runs (it crashes nav-expansion otherwise) and stitched back in after. If you change preprocessor ordering, re-check this dance.
- **ProjectionMapping discipline.** The `_projectionMapping` keys (`ProjectionMember`s) must match exactly what the shaper expects. A mismatch between the post-processor mapping and the shaper compilation produces silent wrong-results, not crashes.
- **`MongoQueryExpression.CapturedExpression`** must be a *complete* method chain — set once at the tail of `MongoQueryableMethodTranslatingExpressionVisitor.VisitMethodCall`. Setting it mid-chain truncates the query.
- **Reference-equality on `MethodInfo`.** Translators that match by `MethodInfo` must use canonical constants — `QueryableMethods` for the top-level dispatch in `MongoQueryableMethodTranslatingExpressionVisitor`, `EnumerableMethods` inside the projection-binding visitors, and the driver's `*Method` reflection classes where the bridge to driver-LINQ needs them; open vs. constructed generic methods compare unequal.
- **Unsupported shapes are detected, not silently translated.** Two layers: the base visitor early-fails truly untranslatable shapes; and for the *native* path, anything `NativeSlotPopulator` does not lower into a slot calls `mongoQ.Select.MarkNotNativelyRepresentable()` (sets the internal `_hasUnsupportedOperator` flag, which drives `Route` to `NativeRoute.Fallback`) so the gate falls back (or throws under `NativeOnly`) rather than emitting a pipeline that silently drops the operator.
- **The native catch-all whitelist must stay in sync.** `NativeSlotPopulator.PopulateNativeSlots` lowers seven slot operators (`Where`/`OrderBy`/`OrderByDescending`/`ThenBy`/`ThenByDescending`/`Skip`/`Take`) into ops on `MongoSelectDefinition`'s ordered `PipelineOps` list (`MongoMatchOp`/`MongoSortOp`/`MongoSkipOp`/`MongoLimitOp` — replacing the original single `Predicate`/`Orderings`/`Offset`/`Limit` slots as of EF-347; see the Non-canonical Skip/Take as-built note above). The `else` catch-all in `PopulateNativeSlots` marks **everything else** non-native by default (correctness-safe: worst case is a missed optimization + fallback, never a wrong result), *unless* `IsNativeRepresentableSlotOperator` returns `true` for the operator. That predicate lists the seven slot operators **plus** the `Translate*`-override operators (`Select`, `OfType`, `Distinct`, `Union`, `Concat`, `SelectManyWithCollectionSelector`) **plus** the scalar-cardinality / `GroupBy` operators added by later sub-projects (`Count`/`LongCount`/`Sum`/`Min`/`Max`/`Average`/`Any`/`All`, the `First`/`Single` family, and `GroupBy*` — EF-336/EF-344/EF-347). Membership is exactly what suppresses the catch-all — for the seven slot operators it means "already populated into a slot," and for the `Translate*`-override operators it means "decided by its own `Translate*` override, don't clobber it." `Union`/`Concat` (EF-347 slice 2) follow the same pattern as `Select`/`OfType`/`Distinct`: `TryTranslateSetOperation` (backing both) always decides representability itself (attaching a `SetOperation` or calling `MarkNotNativelyRepresentable()`), so both are whitelisted here purely to stop the catch-all from re-deciding — see the set-ops as-built note above for the full mechanism. The QMTEV's `TranslateSelect` override calls `NativeProjectionBinder.TryPopulateNativeProjection`, which populates `MongoSelectDefinition.Projection` for a representable shape (terminal member-access anonymous/DTO projection) — leaving `Route` to resolve to `NativeRoute.Projection` — and otherwise calls `MarkNotNativelyRepresentable()`; `TranslateOfType` (EF-347) builds a discriminator `$eq`/`$in` conjunct via `TryBuildDiscriminatorPredicate` and adds it to the tail `$match` via `AddPredicateConjunct` for a representable TPH narrowing (the native DOM shaper already materializes derived types polymorphically via EF's own `MaterializationCondition`, so the conjunct is the only piece native was missing), falling back to `MarkNotNativelyRepresentable()` only when the target type has no discriminator property (non-TPH); `TranslateDistinct` (EF-347 Task 2) follows the same pattern — it calls `NativeGroupByBinder.TryBindDistinctFromProjection` to convert an already-populated `Projection` into a degenerate `Grouping` (key-only, zero accumulators), falling back to `MarkNotNativelyRepresentable()` when there is no native `Projection` to convert (bare-scalar or whole-entity `Distinct`) or a projected key fails `HasDefaultKeySerialization`. `TranslateSelectMany` (EF-347 slices 3–4, the 2-arg collection-selector + result-selector overload EF's nav-expansion always normalizes to) is whitelisted here for the same reason — it first tries `NativeSelectManyBinder.TryBindBareNavUnwind` (the slice-4 bare-nav shape, setting `UnwindSource` only) and, failing that, `NativeSelectManyBinder.TryBind` (the slice-3 inner-`Select` shape, setting `UnwindSource` + `Projection` together) to decide representability itself, but unlike every other `Translate*`-override operator listed here, EITHER binder returning `false` does **not** call `MarkNotNativelyRepresentable()` at all — `TranslateSelectMany` just returns `null`, so an unsupported SelectMany shape reaches EF Core's own translation-failure path directly rather than the graceful-fallback machinery this whitelist otherwise protects (see the SelectMany as-built note above for why that is intentional, not an oversight). The slice-4 bare-nav shape's real projection is bound later still, by the SEPARATE `TranslateSelect` pending-SelectMany branch described in the post-terminal-invariant note above — that branch is not part of this whitelist at all, since it runs inside the already-whitelisted `Select` operator's own override. **Adding a new native operator means:** for a slot-style operator, update BOTH `NativeSlotPopulator.PopulateNativeSlots` (populate the slot) *and* `IsNativeRepresentableSlotOperator` (add it to the whitelist); for a `Translate*`-override-style operator (like `Select`/`OfType`), add its override *and* add it to `IsNativeRepresentableSlotOperator` so the catch-all doesn't override its decision. Miss either path and the operator is silently dropped on the native pipeline or double-decided.
- **The QMTEV `Translate{Where,OrderBy,ThenBy,Skip,Take}` overrides are inert (`=> null`).** Native slot population is delegated to `NativeSlotPopulator.PopulateNativeSlots` at the `VisitMethodCall` fall-through, because routing these through `base.VisitMethodCall` rebuilds a fresh `MongoQueryExpression` per operator (slots don't accumulate). **Do not add these operators to the `VisitMethodCall` switch** without first removing their `NativeSlotPopulator.PopulateNativeSlots` handling — otherwise slots double-populate.
- **The lowerer is BSON-free; the renderer/factory own BSON.** `MongoSelectLowerer` produces only typed `MongoPipelineStage`s; all `BsonDocument` construction lives in `MongoQueryLanguageRenderer` (the `$match` body) and `MongoPipelineFactory.Create` (the stage-walk: `{$match}`/`$sort`/`$skip`/`$limit`/`$lookup`/`$unwind`).
- **Composite-PK key access is NOT native** (strict spike parity) — `MongoExpressionTranslator` rejects multi-property-PK member access, so such predicates/sort fall back. `LookupExpression.GetFieldPath` (`_id.<prop>`) exists for the (still-deferred) composite-PK Include `$lookup` only.
- **Non-positive paging matches EF's contract.** `MongoPipelineFactory.Build` validates `$limit > 0` / `$skip ≥ 0` and throws `ArgumentOutOfRangeException` (client-side, like the driver) — do not emit `{$limit: 0}` (MongoDB rejects it server-side). Property-less primitive values (Skip/Take counts have `forSerialization == null`) serialize via `BsonValue.Create`, not a property serializer.
- **MQL shape cannot prove a query went native.** For filter/sort/paging the native and driver-LINQ pipelines are structurally identical (`$match`/`$sort`/`$skip`/`$limit`), so asserting those substrings under `Native` mode passes even on fallback. The **only reliable "goes native" signal is `NativeOnly` mode**: native-capable ⇒ succeeds; otherwise ⇒ throws `NativeTranslationNotSupportedException`. (Several early gate tests were false positives masked by structurally-identical fallback MQL.)
- **EF Core query cache.** Compiled queries are cached by EF Core by expression-tree shape; if you change a translator's output for a previously-translatable tree, you've quietly invalidated user caches.
- **Multi-EF guards.** Some visitor signatures changed between EF8/EF9/EF10. For representative guard shapes elsewhere in the tree see `Storage/MongoTypeMappingSource.cs` (`#if EF8 || EF9`), the `ChangeTracking/StringDictionaryComparer*.cs` pair (legacy vs. EF10 split), and the `ChangeTracking/ListOf*Comparer.cs` files (`#if EF8`); in Query itself, `QueryingEnumerable.cs` has a `#if !EF8` block.

## How to test

Most query tests assert the **rendered MQL pipeline** rather than executing against a server. The functional and specification tests both have helpers (`AssertMql(...)`) that capture the MQL via `TestMqlLoggerFactory`. Unit tests use **plain xUnit `Assert.*`** (FluentAssertions is not referenced anywhere in the test projects).

**Proving the native path.** `AssertMql` shape does **not** distinguish native from driver-LINQ for filter/sort/paging (identical pipelines) — to assert a query genuinely goes native, run it under `MongoQueryMode.NativeOnly` and assert it **succeeds** (a fallback shape would throw `NativeTranslationNotSupportedException`); to assert a shape falls back, assert it **throws** under `NativeOnly` (or, for `Include`, assert the driver `_outer`/`_inner` shape under `Native`). The spec suite has a **test-only** coverage instrument: `MONGODB_EF_NATIVE_ONLY=1` flips all spec contexts to `NativeOnly` (via `MongoTestStore.AddProviderOptions`), so the pass/fail set is the "what runs native" report. Native unit tests live under `tests/MongoDB.EntityFrameworkCore.UnitTests/Query/NativeTranslation/`; the `QueryModeGate*` functional tests exercise the gate end-to-end.

```bash
dotnet test MongoDB.EFCoreProvider.sln -c "Debug EF10" --no-build \
  --filter "FullyQualifiedName~Query"
```

- Unit tests: `tests/MongoDB.EntityFrameworkCore.UnitTests/Query/`
- Functional tests (real DB): `tests/MongoDB.EntityFrameworkCore.FunctionalTests/Query/` — golden-path translations and end-to-end materialization.
- Specification tests (EF Core conformance, Northwind suite): `tests/MongoDB.EntityFrameworkCore.SpecificationTests/Query/` — pattern is to override the upstream test and assert the produced MQL.
